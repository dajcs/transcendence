---
quick_id: 260427-i85
status: complete
date: 2026-04-27
commit: bd03d13
---

# Quick Task 260427-i85 Summary

## Result

The bug report was valid. `_finalize_uncontested_resolutions` set a market to `closed` before calling `trigger_payout`, so `trigger_payout` treated it as already paid and returned early.

## Changes

- Updated `backend/app/workers/tasks/resolution.py` so uncontested finalization no longer marks the market closed before payout. It now closes the scanner session transaction, then lets `trigger_payout` atomically set `closed`, `winning_side`, `closed_at`, and credit winners.
- Added `test_uncontested_resolution_finalizer_pays_winners` in `backend/tests/test_resolution.py` covering overdue proposer-resolved markets with no dispute.

## Verification

- `DATABASE_URL=sqlite+aiosqlite:///:memory: SECRET_KEY=test-secret JWT_PRIVATE_KEY_PATH=/tmp/missing JWT_PUBLIC_KEY_PATH=/tmp/missing uv run pytest backend/tests/test_resolution.py::test_uncontested_resolution_finalizer_pays_winners backend/tests/test_resolution.py::test_trigger_payout_closes_binary_market_and_records_surplus_and_tp -q` passed.
- `DATABASE_URL=sqlite+aiosqlite:///:memory: SECRET_KEY=test-secret JWT_PRIVATE_KEY_PATH=/tmp/missing JWT_PUBLIC_KEY_PATH=/tmp/missing uv run pytest backend/tests/test_resolution.py -q` passed: 12 tests.

Last action: committed fix `bd03d13`.
