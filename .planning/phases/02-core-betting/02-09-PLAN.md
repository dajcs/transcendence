---
phase: 02-core-betting
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/app/(protected)/dashboard/page.tsx
autonomous: true
gap_closure: true
requirements: [BET-07]

must_haves:
  truths:
    - "Active bet rows have no explicit View label — the row itself is the affordance"
    - "Each active bet row shows the user's own winning probability for their side"
  artifacts:
    - path: "frontend/src/app/(protected)/dashboard/page.tsx"
      provides: "Clean portfolio rows with own-side win probability"
      contains: "win_pct"
  key_links:
    - from: "position.side"
      to: "win probability display"
      via: "side === 'yes' ? yes_pct : no_pct"
---

<objective>
Clean up dashboard portfolio rows.

Purpose: UAT Test 11 found a redundant "View →" label (row is already a Link) and missing own-side win probability. Both are frontend-only fixes — no backend changes needed.
Output: Dashboard rows show win probability for user's position side; no redundant label.
</objective>

<execution_context>
@/mnt/c/Users/dajcs/code/transcendence/.claude/get-shit-done/workflows/execute-plan.md
@/mnt/c/Users/dajcs/code/transcendence/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md

<interfaces>
From frontend/src/app/(protected)/dashboard/page.tsx (current, lines 34-48):
```tsx
<Link
  key={position.id}
  href={`/markets/${position.bet_id}`}
  className="block rounded border border-gray-200 bg-white p-3 hover:border-gray-300"
>
  <div className="flex items-center justify-between">
    <p className="font-medium text-gray-900">{position.market_title}</p>
    <span className="text-xs text-gray-500">View →</span>  {/* REMOVE THIS */}
  </div>
  <p className="mt-1 text-sm text-gray-600">
    {position.side.toUpperCase()} · {position.bp_staked} BP staked ·{" "}
    YES {position.yes_pct}% / NO {position.no_pct}%  {/* REPLACE with own-side % */}
  </p>
</Link>
```

BetPositionWithMarket (from types.ts) has both yes_pct and no_pct available. Win probability for user's side:
```typescript
const winPct = position.side === "yes" ? position.yes_pct : position.no_pct;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove View label and show own-side win probability</name>
  <files>frontend/src/app/(protected)/dashboard/page.tsx</files>
  <action>
Two targeted changes to the active bets render block:

1. Remove `<span className="text-xs text-gray-500">View →</span>` from line 42 (inside the flex justify-between div). The title paragraph remains; the span is deleted. The flex div still works as layout for the title.

2. Replace `YES {position.yes_pct}% / NO {position.no_pct}%` in the subtitle paragraph with a single own-side win probability:
```tsx
<p className="mt-1 text-sm text-gray-600">
  {position.side.toUpperCase()} · {position.bp_staked} BP staked ·{" "}
  Win {position.side === "yes" ? position.yes_pct : position.no_pct}%
</p>
```

The outer Link and all other markup stays exactly as-is.
  </action>
  <verify>
    <automated>cd /mnt/c/Users/dajcs/code/transcendence && ! grep -q "View →" frontend/src/app/(protected)/dashboard/page.tsx && grep -n "Win" frontend/src/app/(protected)/dashboard/page.tsx</automated>
  </verify>
  <done>"View →" span is absent from the file; "Win {x}%" appears in the active bet row markup</done>
</task>

</tasks>

<verification>
- Dashboard active bet rows no longer contain "View →" text
- Each active bet row shows "Win X%" where X is yes_pct if side=yes, no_pct if side=no
- Rows remain clickable (Link wrapper unchanged)
</verification>

<success_criteria>
- No "View →" label in dashboard active bets section
- Own-side win probability visible on each active portfolio entry
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-betting/02-09-SUMMARY.md`
</output>
