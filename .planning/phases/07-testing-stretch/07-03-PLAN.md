---
phase: "07"
plan: "03"
type: execute
wave: 2
depends_on: ["07-01", "07-02"]
files_modified:
  - frontend/package.json
  - frontend/playwright.config.ts
  - frontend/e2e/auth.spec.ts
  - frontend/e2e/bet-lifecycle.spec.ts
  - frontend/e2e/dispute.spec.ts
  - frontend/e2e/notifications.spec.ts
  - backend/app/api/main.py
  - backend/app/api/routes/test_support.py
  - .github/workflows/ci.yml
  - .github/workflows/e2e-manual.yml
  - Makefile
autonomous: true
requirements: [TEST-04]
user_setup: []
must_haves:
  truths:
    - "The repo has a Playwright-based E2E path for exactly four critical flows: auth, bet lifecycle, dispute, and notifications"
    - "The heavy Docker/Playwright path is optional or manually triggered in CI, not mandatory on every push/PR"
    - "Backend and frontend automated checks run in GitHub Actions on normal push/PR events"
    - "If test-support helpers are introduced, they are clearly gated for test use and are not enabled by default in normal runtime"
  artifacts:
    - path: "frontend/playwright.config.ts"
      provides: "Playwright runtime configuration for Vox Populi"
      contains: "defineConfig"
    - path: "frontend/e2e/auth.spec.ts"
      provides: "Critical auth flow E2E coverage"
      contains: "test("
    - path: "frontend/e2e/bet-lifecycle.spec.ts"
      provides: "Critical market lifecycle E2E coverage"
      contains: "test("
    - path: "frontend/e2e/dispute.spec.ts"
      provides: "Critical dispute flow E2E coverage"
      contains: "test("
    - path: "frontend/e2e/notifications.spec.ts"
      provides: "Critical notification E2E coverage"
      contains: "test("
    - path: ".github/workflows/ci.yml"
      provides: "Normal push/PR backend + frontend automation"
      contains: "pull_request"
    - path: ".github/workflows/e2e-manual.yml"
      provides: "Optional/manual Docker + Playwright workflow"
      contains: "workflow_dispatch"
  key_links:
    - from: "frontend/package.json"
      to: "frontend/playwright.config.ts"
      via: "Playwright scripts and browser install path"
      pattern: "playwright"
    - from: ".github/workflows/ci.yml"
      to: "backend/pyproject.toml and frontend/package.json"
      via: "workflow calls repo-native backend and frontend test commands"
      pattern: "test"
    - from: ".github/workflows/e2e-manual.yml"
      to: "docker-compose.yml"
      via: "manual heavy path runs against the real Docker stack"
      pattern: "docker compose"
---

<objective>
Create the integration wave of Phase 7: Playwright for the four required critical flows, plus GitHub Actions that run lightweight backend/frontend checks on normal events and the heavy Docker/Playwright path only on optional/manual execution.

Purpose: TEST-04 and the CI deliverable belong together because the E2E path only becomes useful once it can be invoked reliably in automation.
Output: Playwright config and four specs, any minimal gated test-support plumbing needed for reliable setup, and two GitHub Actions workflows that reflect the chosen CI strictness.
</objective>

<execution_context>
@/mnt/c/Users/dajcs/code/transcendence/.codex/get-shit-done/workflows/execute-plan.md
@/mnt/c/Users/dajcs/code/transcendence/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-testing-stretch/07-CONTEXT.md
@.planning/phases/07-testing-stretch/07-RESEARCH.md
@docker-compose.yml
@Makefile
@frontend/package.json
@backend/app/api/main.py
@backend/app/api/routes/auth.py
@backend/app/api/routes/bets.py
@backend/app/api/routes/resolution.py
@frontend/src/components/NotificationBell.tsx
@frontend/src/app/(protected)/markets/[id]/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Playwright scaffolding and any minimal gated test-support setup</name>
  <files>frontend/package.json, frontend/playwright.config.ts, backend/app/api/main.py, backend/app/api/routes/test_support.py, Makefile</files>
  <read_first>frontend/package.json, docker-compose.yml, backend/app/api/main.py, .planning/phases/07-testing-stretch/07-CONTEXT.md, .planning/phases/07-testing-stretch/07-RESEARCH.md</read_first>
  <action>
Introduce the minimum scaffolding needed to make Playwright viable in this repo without broadening scope.

Specific changes:
- Add Playwright dependencies/scripts to `frontend/package.json`, including a deterministic browser-install path and an E2E run command.
- Create `frontend/playwright.config.ts` with base URL and runtime settings appropriate for the real app stack.
- If the four required scenarios cannot be set up reliably through existing UI and API flows alone, add a narrow `backend/app/api/routes/test_support.py` helper route set that is explicitly test-only and disabled by default. Register it in `backend/app/api/main.py` behind an env/config gate so it is unavailable in normal runtime.
- Add any matching Makefile target needed to invoke the heavy E2E path consistently.

Do not create an always-on public test helper. If no helper route is needed after inspecting the flows, skip that file and keep setup inside the Playwright specs.
  </action>
  <verify>cd /mnt/c/Users/dajcs/code/transcendence/frontend && npm exec playwright test --list</verify>
  <acceptance_criteria>
    - `frontend/package.json` contains Playwright-related scripts or dependencies
    - `frontend/playwright.config.ts` exists
    - Any test-support backend route is explicitly gated and not exposed by default
    - `Makefile` exposes a concrete E2E invocation path if it is modified
  </acceptance_criteria>
  <done>The repo has a Playwright-capable scaffolding path and any optional backend test helper is clearly test-only and gated off by default.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the four critical Playwright flows only</name>
  <files>frontend/e2e/auth.spec.ts, frontend/e2e/bet-lifecycle.spec.ts, frontend/e2e/dispute.spec.ts, frontend/e2e/notifications.spec.ts</files>
  <read_first>frontend/playwright.config.ts, frontend/src/components/NotificationBell.tsx, frontend/src/app/(protected)/markets/[id]/page.tsx, backend/app/api/routes/auth.py, backend/app/api/routes/bets.py, backend/app/api/routes/resolution.py</read_first>
  <action>
Implement exactly four E2E specs and no more:
- `auth.spec.ts`
- `bet-lifecycle.spec.ts`
- `dispute.spec.ts`
- `notifications.spec.ts`

Each spec should target a real critical path with clear assertions on visible outcomes. Prefer stable setup over exhaustive variation coverage. Do not add generic smoke suites, broad navigation sweeps, or non-critical permutations.

Where possible, use reusable setup helpers inside the E2E test layer so the four flows are maintainable. If notifications or dispute setup require multiple browser contexts/users, make that explicit and deterministic in the spec helpers.
  </action>
  <verify>cd /mnt/c/Users/dajcs/code/transcendence/frontend && npm run test:e2e</verify>
  <acceptance_criteria>
    - `frontend/e2e/auth.spec.ts` exists
    - `frontend/e2e/bet-lifecycle.spec.ts` exists
    - `frontend/e2e/dispute.spec.ts` exists
    - `frontend/e2e/notifications.spec.ts` exists
    - No extra non-critical Playwright suites are introduced in this phase
  </acceptance_criteria>
  <done>The repo has Playwright coverage for exactly the four required critical flows and nothing broader.</done>
</task>

<task type="auto">
  <name>Task 3: Add incremental GitHub Actions CI with optional/manual heavy E2E path</name>
  <files>.github/workflows/ci.yml, .github/workflows/e2e-manual.yml</files>
  <read_first>.planning/phases/07-testing-stretch/07-CONTEXT.md, .planning/phases/07-testing-stretch/07-RESEARCH.md, backend/pyproject.toml, frontend/package.json, docker-compose.yml</read_first>
  <action>
Create two workflows that match the user's CI strictness decision.

Workflow 1: `.github/workflows/ci.yml`
- Trigger on `push` and `pull_request`
- Run backend and frontend automated checks only
- Use repo-native commands/scripts rather than duplicating long command logic in YAML

Workflow 2: `.github/workflows/e2e-manual.yml`
- Trigger on `workflow_dispatch` (and optionally another non-default trigger if useful)
- Boot the Docker stack and run the heavy Playwright path
- Shut the stack down cleanly afterward

Do not make Docker/Playwright a required path on every PR in this phase.
  </action>
  <verify>grep -n "pull_request\\|push" /mnt/c/Users/dajcs/code/transcendence/.github/workflows/ci.yml && grep -n "workflow_dispatch" /mnt/c/Users/dajcs/code/transcendence/.github/workflows/e2e-manual.yml</verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` exists and triggers on `push` and `pull_request`
    - `.github/workflows/ci.yml` runs backend and frontend checks without requiring the Docker/Playwright path
    - `.github/workflows/e2e-manual.yml` exists and triggers on `workflow_dispatch`
    - The heavy Docker/Playwright workflow is optional/manual, not always-on for normal PRs
  </acceptance_criteria>
  <done>GitHub Actions provides lightweight default automation and a separate optional/manual heavy E2E path that matches the locked CI decision.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd frontend && npm exec playwright test --list`
- [ ] `.github/workflows/ci.yml` validates as a normal push/PR workflow
- [ ] `.github/workflows/e2e-manual.yml` validates as a manual heavy-path workflow
- [ ] Any test-support backend route remains disabled by default outside the E2E context
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- TEST-04 is satisfied by exactly four critical Playwright flows
- CI reflects the chosen incremental strictness: light checks on normal events, heavy E2E optional/manual
</success_criteria>

<output>
After completion, create `.planning/phases/07-testing-stretch/07-03-SUMMARY.md`
</output>
