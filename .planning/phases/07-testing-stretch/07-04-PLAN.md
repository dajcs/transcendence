---
phase: 07-testing-stretch
plan: 04
type: execute
wave: 3
depends_on: ["07-01", "07-02", "07-03"]
files_modified:
  - Makefile
  - .github/workflows/ci.yml
  - .github/workflows/e2e-manual.yml
autonomous: true
gap_closure: true
requirements: [TEST-01, TEST-02, TEST-03, TEST-04]
must_haves:
  truths:
    - "Phase 7 proof commands run from repo-owned entrypoints instead of depending on the broken checked-in backend virtualenv or preinstalled frontend node_modules"
    - "Backend pytest coverage proof uses the existing SQLite plus fakeredis baseline and a writable repo-local uv cache path"
    - "Frontend Jest and Playwright proof commands install their own local dependencies before execution and no longer assume command surfaces already exist"
    - "GitHub Actions keeps lightweight checks on push/pull_request and keeps Docker plus Playwright manual-only while calling the same repo-native command surfaces"
    - "Execution evidence can record real backend/frontend coverage output and E2E command success for TEST-01 through TEST-04 without hand-waving around environment defects"
  artifacts:
    - path: "Makefile"
      provides: "Repo-native proof targets for backend coverage, frontend coverage, and E2E preparation/execution"
      contains: "phase7"
    - path: ".github/workflows/ci.yml"
      provides: "Incremental CI workflow for normal push and pull_request checks"
      contains: "pull_request"
    - path: ".github/workflows/e2e-manual.yml"
      provides: "Manual Docker plus Playwright workflow for the heavy path"
      contains: "workflow_dispatch"
    - path: ".planning/phases/07-testing-stretch/07-04-SUMMARY.md"
      provides: "Recorded proof outputs and any remaining external prerequisite notes"
      contains: "coverage"
  key_links:
    - from: "Makefile"
      to: "backend/pyproject.toml"
      via: "repo-local uv sync/run targets using a writable UV_CACHE_DIR"
      pattern: "UV_CACHE_DIR"
    - from: "Makefile"
      to: "frontend/package.json"
      via: "npm ci plus Jest and Playwright scripts"
      pattern: "test:coverage|test:e2e"
    - from: ".github/workflows/ci.yml"
      to: "Makefile"
      via: "push/pull_request jobs call repo-owned proof targets instead of duplicating ad hoc shell logic"
      pattern: "make"
    - from: ".github/workflows/e2e-manual.yml"
      to: "Makefile"
      via: "workflow_dispatch heavy path uses the same prepared Docker plus Playwright command surface"
      pattern: "make"
---

<objective>
Close the Phase 7 verification gaps without replanning the completed testing work. Preserve the existing test stack and artifacts from 07-01 through 07-03, then add repo-contained proof entrypoints that make backend/frontend verification runnable again and align the already-present CI workflows with those entrypoints.

Purpose: `07-VERIFICATION.md` failed mostly on proof and environment drift, not on missing test implementation. This plan fixes the proof path and CI wiring so TEST-01 through TEST-04 can be re-verified using the current repo reality.
Output: A hardened `Makefile` proof surface plus CI workflows that call it, allowing real coverage and E2E evidence to be captured without depending on the broken checked-in `.venv` or preexisting `frontend/node_modules`.
</objective>

<execution_context>
@/mnt/c/Users/dajcs/code/transcendence/.codex/get-shit-done/workflows/execute-plan.md
@/mnt/c/Users/dajcs/code/transcendence/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-testing-stretch/07-CONTEXT.md
@.planning/phases/07-testing-stretch/07-VERIFICATION.md
@.planning/phases/07-testing-stretch/07-01-SUMMARY.md
@.planning/phases/07-testing-stretch/07-02-SUMMARY.md
@.planning/phases/07-testing-stretch/07-03-SUMMARY.md
@Makefile
@backend/pyproject.toml
@frontend/package.json
@frontend/playwright.config.ts
@.github/workflows/ci.yml
@.github/workflows/e2e-manual.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add repo-owned proof targets that do not rely on stale local state</name>
  <files>Makefile</files>
  <action>
Add a focused Phase 7 proof surface to `Makefile` so local and CI verification use the same entrypoints.

Implement all of the following:
- Add backend sync/run targets that use a repo-local writable cache path such as `UV_CACHE_DIR=$(CURDIR)/.cache/uv` or equivalent, per D-01, D-03, and the backend failure recorded in `07-VERIFICATION.md`.
- Route backend proof through `uv sync --group dev` and `uv run pytest ...`; do not repair or depend on `backend/.venv/bin/python`, because the checked-in symlink is local state and already proven broken.
- Add frontend install/proof targets that start from `npm ci` inside `frontend/`, per D-01 and the missing `frontend/node_modules` command surfaces in `07-VERIFICATION.md`.
- Reuse the existing Jest and Playwright scripts from `frontend/package.json`; do not migrate away from Jest and do not add a second frontend test runner.
- Add one aggregate target for the lightweight proof path and one target for the heavy Docker-backed path so the commands are easy for CI and humans to invoke consistently.

Be explicit in target names that these are Phase 7 proof commands. Do not broaden scope into new tests or stretch work.
  </action>
  <verify>
    <automated>cd /mnt/c/Users/dajcs/code/transcendence && make phase7-proof-backend && make phase7-proof-frontend && make phase7-proof-e2e-list</automated>
  </verify>
  <done>Repo-owned proof commands exist for backend coverage, frontend coverage, and Playwright preparation/listing without depending on the broken checked-in backend venv or preinstalled frontend packages.</done>
</task>

<task type="auto">
  <name>Task 2: Align the existing CI workflows to the repo proof surface</name>
  <files>.github/workflows/ci.yml, .github/workflows/e2e-manual.yml, Makefile</files>
  <action>
Treat the workflow files as existing baseline artifacts, not as missing greenfield files. Inspect the current `.github/workflows/ci.yml` and `.github/workflows/e2e-manual.yml`, keep the incremental CI structure from D-10 and D-11, and tighten them so they call the repo-owned Phase 7 proof targets from Task 1.

Specific requirements:
- `ci.yml` must stay on `push` and `pull_request`, remain lightweight, and call backend/frontend proof targets only.
- `e2e-manual.yml` must stay `workflow_dispatch`, remain the heavy Docker plus Playwright path, and call the heavy proof target rather than duplicating setup logic in multiple places where practical.
- Preserve the current stack reality from D-01 through D-04: backend proof remains pytest with SQLite plus fakeredis; frontend proof remains Jest plus Playwright.
- Keep repo-contained fixes in the repo and label true external prerequisites explicitly: Docker availability is an external prerequisite for the heavy path; Node, npm, uv, and GitHub runner setup are expected tooling, not reasons to hand-wave verification away.
- Do not make Playwright mandatory on every push/PR, and do not add stretch-module work.

If the workflows already satisfy part of the gap, keep those sections and only change the wiring needed to close the verification failure cleanly.
  </action>
  <verify>
    <automated>cd /mnt/c/Users/dajcs/code/transcendence && grep -n "push\\|pull_request" .github/workflows/ci.yml && grep -n "workflow_dispatch" .github/workflows/e2e-manual.yml && rg -n "make phase7-proof|make phase7-heavy" .github/workflows/ci.yml .github/workflows/e2e-manual.yml</automated>
  </verify>
  <done>The CI workflows remain incremental and now call the same repo-owned proof targets used for local verification, with the heavy E2E path still manual-only.</done>
</task>

<task type="auto">
  <name>Task 3: Capture real proof for TEST-01 through TEST-04 and document any true external blocker precisely</name>
  <files>Makefile, .planning/phases/07-testing-stretch/07-04-SUMMARY.md</files>
  <action>
Use the Task 1 command surface to rerun the previously blocked proof and capture actual results in the execution summary.

Required proof pass:
- Run the backend proof target and record the real pytest plus coverage output for TEST-01 and TEST-02.
- Run the frontend proof target and record the real Jest coverage output for TEST-03.
- Run the Playwright listing/preparation target to prove local command surfaces exist.
- Obtain TEST-04 proof through one of these concrete paths and record which one was used in `07-04-SUMMARY.md`:
  - local Docker-capable execution via `make phase7-heavy`, or
  - a successful `workflow_dispatch` run of `.github/workflows/e2e-manual.yml`, triggered and then observed to completion during execution, with the run URL/id and final success status captured in the summary.

Documentation requirements for the summary:
- Separate repo-contained fixes from external prerequisites explicitly.
- If local Docker is unavailable in the executor environment, the task must use the manual GitHub Actions workflow path instead of stopping short. Treat GitHub CLI auth/repo access as an execution prerequisite for that alternate path and document it before dispatching.
- The summary must include one explicit TEST-04 proof section with:
  - `Proof source: local-docker` or `Proof source: github-actions`
  - the exact command or workflow run identifier used
  - a success marker for the chosen path
- Do not claim coverage thresholds are met without including the actual command outputs or percentages observed during execution.
- Do not reopen or rewrite 07-01 through 07-03 unless a proof command exposes a concrete defect in those artifacts.
  </action>
  <verify>
    <automated>cd /mnt/c/Users/dajcs/code/transcendence && make phase7-proof && ( make phase7-heavy || rg -n "Proof source: github-actions|Workflow run:|Workflow status: success" .planning/phases/07-testing-stretch/07-04-SUMMARY.md )</automated>
  </verify>
  <done>Phase 7 has real rerun evidence for backend/frontend coverage and a concrete TEST-04 proof record from either local Docker execution or the manual GitHub Actions E2E workflow.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions runner -> repo commands | Untrusted runner environment executes repo-maintained proof targets and must not drift from local verification behavior |
| Test-only env flag -> backend runtime | `ENABLE_E2E_TEST_SUPPORT` exposes seeded E2E helpers only when explicitly enabled |
| Local shell -> dependency installers | `uv sync` and `npm ci` pull toolchain dependencies and must write only to intended workspace-local locations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04-01 | T | `Makefile` proof targets | mitigate | Force repo-local cache/install paths for uv and Playwright so proof commands do not depend on or corrupt stale global/local state. |
| T-07-04-02 | E | `backend/app/api/routes/test_support.py` via manual E2E flow | mitigate | Preserve the existing env gate and keep heavy E2E execution manual-only so seeded test helpers are never enabled by default runtime paths. |
| T-07-04-03 | R | `.github/workflows/ci.yml` and `.github/workflows/e2e-manual.yml` | mitigate | Make workflows call repo-owned `make` targets so command changes are traceable in one place and execution evidence matches local proof commands. |
| T-07-04-04 | I | workflow logs and generated env files | mitigate | Avoid echoing secrets, keep env preparation limited to test-safe values, and upload only intended Playwright report artifacts. |
</threat_model>

<verification>
- `cd /mnt/c/Users/dajcs/code/transcendence && make phase7-proof-backend`
- `cd /mnt/c/Users/dajcs/code/transcendence && make phase7-proof-frontend`
- `cd /mnt/c/Users/dajcs/code/transcendence && make phase7-proof-e2e-list`
- `cd /mnt/c/Users/dajcs/code/transcendence && make phase7-heavy` in a Docker-capable environment, or record the exact external blocker if Docker is unavailable
</verification>

<success_criteria>
- The existing CI workflows remain incremental and are not rewritten into an always-on heavy pipeline.
- Backend proof no longer depends on `backend/.venv` and uses a writable repo-local `uv` cache path.
- Frontend proof no longer assumes existing local binaries and starts from `npm ci`.
- Phase 7 can be re-verified with real command output for TEST-01 through TEST-04, with TEST-04 evidenced either locally via Docker or through the manual GitHub Actions workflow.
</success_criteria>

<output>
After completion, create `.planning/phases/07-testing-stretch/07-04-SUMMARY.md`
</output>
