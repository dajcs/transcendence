---
phase: 07-testing-stretch
plan: "05"
type: execute
wave: 4
depends_on: ["07-04"]
files_modified:
  - backend/pyproject.toml
  - Makefile
  - backend/tests/test_economy.py
  - backend/tests/test_resolution.py
  - backend/tests/test_markets.py
  - backend/tests/test_comments.py
  - backend/tests/test_auth.py
  - backend/tests/test_friends.py
  - backend/tests/test_chat.py
  - backend/tests/test_gdpr.py
  - backend/tests/test_llm_routes.py
  - backend/tests/test_tasks.py
  - frontend/jest.config.ts
  - frontend/package.json
  - frontend/src/components/auth/__tests__/RegisterForm.test.tsx
  - frontend/src/components/auth/__tests__/ResetForm.test.tsx
  - frontend/src/components/auth/__tests__/OAuthButtons.test.tsx
  - frontend/src/components/nav/__tests__/Sidebar.test.tsx
  - frontend/src/components/__tests__/ThemeProvider.test.tsx
  - frontend/src/components/__tests__/UserSearch.test.tsx
  - frontend/src/app/(protected)/markets/__tests__/markets-page.test.tsx
  - frontend/src/app/(protected)/settings/__tests__/settings-page.test.tsx
  - frontend/src/store/__tests__/auth-store.test.ts
  - frontend/src/store/__tests__/notifications-store.test.ts
autonomous: true
gap_closure: true
requirements: [TEST-01, TEST-02, TEST-03]
must_haves:
  truths:
    - "Backend coverage proof reaches at least 80 percent total coverage and the proof target fails below that threshold"
    - "Economy formula coverage reaches 100 percent for backend/app/services/economy_service.py or an explicitly documented narrower economy-formula file target"
    - "Frontend Jest coverage proof reaches at least 70 percent for the configured all-files coverage set and the proof target fails below that threshold"
    - "Coverage uplift comes from meaningful regression tests for existing product behavior, not from broad exclusions of product code"
    - "Previously reviewed permissive tests for market creation and comment upvotes are tightened so they can fail on the behavior they claim to protect"
  artifacts:
    - path: "backend/pyproject.toml"
      provides: "Backend coverage fail-under configuration"
      contains: "fail_under = 80"
    - path: "Makefile"
      provides: "Phase 7 proof targets that enforce backend, economy, and frontend coverage thresholds"
      contains: "phase7-proof-backend"
    - path: "frontend/jest.config.ts"
      provides: "Jest coverageThreshold enforcing the TEST-03 coverage contract"
      contains: "coverageThreshold"
    - path: "backend/coverage.xml"
      provides: "Backend coverage proof at or above the required threshold"
      contains: "line-rate"
    - path: "frontend/coverage/lcov.info"
      provides: "Frontend Jest coverage proof at or above the required threshold"
      contains: "end_of_record"
  key_links:
    - from: "Makefile"
      to: "backend/pyproject.toml"
      via: "phase7-proof-backend runs pytest coverage with fail-under enabled"
      pattern: "cov-fail-under|fail_under"
    - from: "Makefile"
      to: "backend/app/services/economy_service.py"
      via: "economy-specific coverage proof prevents TEST-01 from being hidden by total coverage"
      pattern: "economy"
    - from: "frontend/jest.config.ts"
      to: "frontend/package.json"
      via: "npm run test:coverage fails below the configured TEST-03 threshold"
      pattern: "coverageThreshold|test:coverage"
---

<objective>
Close the remaining Phase 7 verification gaps by raising backend and frontend coverage to the formal thresholds and making the proof commands fail when coverage drops below those thresholds.

Purpose: `07-VERIFICATION.md` now proves that command surfaces, CI, and E2E are wired, but backend coverage is below the 80 percent contract and frontend Jest coverage is below the 70 percent contract. This plan adds targeted regression tests and threshold enforcement without changing the chosen pytest/Jest stack or reopening the already-verified Playwright scope.
Output: Passing `make phase7-proof-backend` and `make phase7-proof-frontend` runs that enforce the Phase 7 coverage requirements, plus a summary with the exact backend and frontend coverage percentages.
</objective>

<execution_context>
@/mnt/c/Users/dajcs/code/transcendence/.codex/get-shit-done/workflows/execute-plan.md
@/mnt/c/Users/dajcs/code/transcendence/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-testing-stretch/07-CONTEXT.md
@.planning/phases/07-testing-stretch/07-VERIFICATION.md
@.planning/phases/07-testing-stretch/07-REVIEW.md
@.planning/phases/07-testing-stretch/07-04-SUMMARY.md
@backend/coverage.xml
@frontend/coverage/lcov.info
@backend/pyproject.toml
@frontend/jest.config.ts
@Makefile
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enforce and satisfy backend coverage thresholds</name>
  <files>backend/pyproject.toml, Makefile, backend/tests/test_economy.py, backend/tests/test_resolution.py, backend/tests/test_auth.py, backend/tests/test_friends.py, backend/tests/test_chat.py, backend/tests/test_gdpr.py, backend/tests/test_llm_routes.py, backend/tests/test_tasks.py</files>
  <read_first>backend/coverage.xml, backend/pyproject.toml, Makefile, backend/tests/conftest.py, backend/tests/test_economy.py, backend/tests/test_resolution.py, backend/tests/test_auth.py, backend/app/services/economy_service.py, backend/app/api/routes/resolution.py, backend/app/services/friend_service.py, backend/app/services/chat_service.py, backend/app/services/gdpr_service.py, backend/app/api/routes/llm.py, backend/app/workers/tasks/resolution.py, .planning/phases/07-testing-stretch/07-VERIFICATION.md</read_first>
  <action>
Raise backend coverage from the verified 59.92 percent line-rate / 56 percent terminal total to the roadmap's 80 percent backend coverage contract, then enforce that threshold in the proof path.

Concrete implementation requirements:
- In `backend/pyproject.toml`, set `[tool.coverage.report] fail_under = 80`. Keep `branch = true`, `source = ["app"]`, and the existing pytest stack.
- In `Makefile`, keep `phase7-proof-backend` routed through `uv sync --group dev` and `UV_CACHE_DIR="$(UV_CACHE_DIR)" uv run pytest`. Add or preserve an explicit coverage fail-under path so the target exits nonzero below 80 percent. Acceptable concrete command text includes `--cov-fail-under=80` or relying on `[tool.coverage.report] fail_under = 80` while keeping `--cov=app`.
- Add targeted backend tests for the lowest uncovered product areas shown by `backend/coverage.xml`, prioritizing files that are both product-critical and currently below 80 percent:
  - `backend/app/api/routes/resolution.py` currently 17.14 percent: add route-level tests for proposer resolution, dispute creation/voting, invalid state/error handling, or deadline guard behavior using existing `AsyncClient` patterns.
  - `backend/app/services/friend_service.py` currently 20.63 percent and `backend/app/api/routes/friends.py` currently 65.31 percent: add tests for request, accept, decline/block, duplicate request, and blocked-user guard behavior.
  - `backend/app/services/chat_service.py` currently 15.05 percent and `backend/app/api/routes/chat.py` currently 58.06 percent: add tests for thread creation/listing/message retrieval and unauthorized or invalid-recipient cases.
  - `backend/app/services/gdpr_service.py` currently 16.67 percent: add unit tests for export payload shape and pseudonymization/delete-account behavior that avoid external services.
  - `backend/app/workers/tasks/resolution.py` currently 33.67 percent: add deterministic tests around due-market selection and auto-resolution task branches with service calls mocked where needed.
- Preserve the SQLite plus fakeredis harness from `backend/tests/conftest.py`; do not introduce live Postgres or Redis as a default test prerequisite.
- Add enough economy-service tests so `backend/app/services/economy_service.py` reaches 100 percent line coverage, or document in `07-05-SUMMARY.md` the exact narrower economy-formula target if non-formula lines keep the service file below 100 percent. The preferred target is the full file.
  </action>
  <verify>
    <automated>cd /mnt/c/Users/dajcs/code/transcendence && make phase7-proof-backend</automated>
    <automated>cd /mnt/c/Users/dajcs/code/transcendence/backend && UV_CACHE_DIR="/mnt/c/Users/dajcs/code/transcendence/.cache/uv" uv run coverage report --include="app/services/economy_service.py" --fail-under=100</automated>
  </verify>
  <acceptance_criteria>
    - `backend/pyproject.toml` contains `fail_under = 80`
    - `make phase7-proof-backend` exits 0 only when total backend coverage is at least 80 percent
    - `backend/coverage.xml` root `line-rate` is at least `0.80` after the proof run
    - `coverage report --include="app/services/economy_service.py" --fail-under=100` exits 0, or `07-05-SUMMARY.md` records the exact narrower TEST-01 economy-formula proof and why full-file 100 percent is not the contract used
    - New backend tests use the existing SQLite plus fakeredis test harness and do not require Docker services
  </acceptance_criteria>
  <done>Backend coverage satisfies and enforces TEST-01 and TEST-02 rather than merely reporting below-threshold numbers.</done>
</task>

<task type="auto">
  <name>Task 2: Tighten permissive backend tests found by review</name>
  <files>backend/tests/test_markets.py, backend/tests/test_comments.py</files>
  <read_first>backend/tests/test_markets.py, backend/tests/test_comments.py, backend/app/api/routes/markets.py, backend/app/api/routes/comments.py, backend/app/services/market_service.py, backend/app/services/comment_service.py, .planning/phases/07-testing-stretch/07-REVIEW.md</read_first>
  <action>
Fix the three warning findings from `07-REVIEW.md` so existing backend tests prove the behavior their names describe.

Concrete implementation requirements:
- In `backend/tests/test_markets.py`, change `test_create_market_deducts_1bp` so it reads `/api/auth/me` before and after market creation and asserts `after_bp == before_bp - 1`.
- In `backend/tests/test_markets.py`, change `test_create_market_insufficient_bp` so it creates or mutates a real zero-BP user through the test database/session, submits a valid market payload, and asserts exactly `resp.status_code == 402`. Remove acceptance of `201` and `422`.
- In `backend/tests/test_comments.py`, rename or update `test_upvote_comment_earns_kp` to the current LP terminology, then assert the comment author's LP increases by the expected amount after a different user upvotes the comment. Do not leave the test as a status-code-only assertion.
- Keep these tests deterministic and compatible with the existing async client/session fixtures.
  </action>
  <verify>
    <automated>cd /mnt/c/Users/dajcs/code/transcendence/backend && UV_CACHE_DIR="/mnt/c/Users/dajcs/code/transcendence/.cache/uv" uv run pytest tests/test_markets.py tests/test_comments.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `backend/tests/test_markets.py` contains `after_bp == before_bp - 1`
    - `backend/tests/test_markets.py` no longer contains `resp.status_code in (201, 402, 422)`
    - `backend/tests/test_markets.py` contains `resp.status_code == 402` for the insufficient-BP case
    - `backend/tests/test_comments.py` contains an assertion that the author's `lp` increases after the upvote
    - No test name or assertion uses stale `kp` terminology for Like Points behavior
  </acceptance_criteria>
  <done>The review-identified backend tests now fail if BP deduction, insufficient-BP rejection, or LP upvote rewards regress.</done>
</task>

<task type="auto">
  <name>Task 3: Enforce and satisfy frontend Jest coverage thresholds</name>
  <files>frontend/jest.config.ts, frontend/package.json, frontend/src/components/auth/__tests__/RegisterForm.test.tsx, frontend/src/components/auth/__tests__/ResetForm.test.tsx, frontend/src/components/auth/__tests__/OAuthButtons.test.tsx, frontend/src/components/nav/__tests__/Sidebar.test.tsx, frontend/src/components/__tests__/ThemeProvider.test.tsx, frontend/src/components/__tests__/UserSearch.test.tsx, frontend/src/app/(protected)/markets/__tests__/markets-page.test.tsx, frontend/src/app/(protected)/settings/__tests__/settings-page.test.tsx, frontend/src/store/__tests__/auth-store.test.ts, frontend/src/store/__tests__/notifications-store.test.ts</files>
  <read_first>frontend/coverage/lcov.info, frontend/jest.config.ts, frontend/package.json, frontend/src/components/auth/RegisterForm.tsx, frontend/src/components/auth/ResetForm.tsx, frontend/src/components/auth/OAuthButtons.tsx, frontend/src/components/nav/Sidebar.tsx, frontend/src/components/ThemeProvider.tsx, frontend/src/components/UserSearch.tsx, frontend/src/app/(protected)/markets/page.tsx, frontend/src/app/(protected)/settings/page.tsx, frontend/src/store/auth.ts, frontend/src/store/notifications.ts, frontend/src/__tests__/NotificationBell.test.tsx, frontend/src/app/(protected)/markets/[id]/__tests__/market-detail.test.tsx, .planning/phases/07-testing-stretch/07-VERIFICATION.md</read_first>
  <action>
Raise frontend Jest coverage from the verified 31.91 percent statements / 33.73 percent lines to the TEST-03 70 percent contract, then enforce that threshold in Jest.

Concrete implementation requirements:
- In `frontend/jest.config.ts`, add a `coverageThreshold` global gate that enforces at least `statements: 70` and `lines: 70`. If functions or branches are not set to 70, document the exact lower value and reason in `07-05-SUMMARY.md`; do not omit statements or lines.
- Do not reduce the coverage denominator by excluding product pages/components solely to pass the gate. Existing low-signal exclusions may remain (`layout.tsx`, `providers.tsx`, generated types), but product files such as `markets/page.tsx`, `settings/page.tsx`, `RegisterForm.tsx`, `Sidebar.tsx`, stores, and user-facing components must stay in scope unless a file is a framework wrapper with no behavior.
- Add behavior-focused tests for high-line uncovered frontend files from `frontend/coverage/lcov.info`:
  - `frontend/src/app/(protected)/markets/page.tsx` currently 0/107 lines: test loading, empty/error states, rendered market rows, and navigation/link behavior with mocked `@/lib/markets` or API calls.
  - `frontend/src/app/(protected)/settings/page.tsx` currently 0/78 lines: test settings rendering, form state, save success, and API error handling with mocked API calls.
  - `frontend/src/components/auth/RegisterForm.tsx` currently 0/41 lines: test client validation, successful submit path, and server error display.
  - `frontend/src/components/nav/Sidebar.tsx` currently 0/35 lines: test auth-dependent links, active/current route state, and logout action wiring.
  - `frontend/src/components/UserSearch.tsx` currently 0/39 lines: test search debounce/result rendering, empty results, and selection/link behavior using fake timers where needed.
  - `frontend/src/components/auth/OAuthButtons.tsx`, `ResetForm.tsx`, `ThemeProvider.tsx`, and the Zustand stores currently at 0 percent: add focused tests for observable behavior and state transitions rather than snapshot assertions.
- Preserve Jest plus React Testing Library; do not migrate to Vitest and do not broaden Playwright scope.
  </action>
  <verify>
    <automated>cd /mnt/c/Users/dajcs/code/transcendence && make phase7-proof-frontend</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/jest.config.ts` contains `coverageThreshold`
    - `frontend/jest.config.ts` contains `statements: 70` and `lines: 70`
    - `make phase7-proof-frontend` exits 0 only when the configured frontend coverage threshold is met
    - `frontend/coverage/lcov.info` computes at least 70 percent line coverage after the proof run
    - Added frontend tests are behavior-focused and contain no snapshot assertions
  </acceptance_criteria>
  <done>Frontend Jest coverage satisfies and enforces TEST-03 with meaningful tests over user-facing behavior.</done>
</task>

<task type="auto">
  <name>Task 4: Record final coverage proof and preserve verified E2E scope</name>
  <files>.planning/phases/07-testing-stretch/07-05-SUMMARY.md, .planning/phases/07-testing-stretch/07-VERIFICATION.md</files>
  <read_first>.planning/phases/07-testing-stretch/07-04-SUMMARY.md, .planning/phases/07-testing-stretch/07-VERIFICATION.md, .planning/phases/07-testing-stretch/07-VALIDATION.md, Makefile, backend/coverage.xml, frontend/coverage/lcov.info</read_first>
  <action>
Capture the exact final proof for the remaining Phase 7 gaps and do not reopen already verified TEST-04 scope.

Concrete implementation requirements:
- Run `make phase7-proof-backend` and record the final backend coverage percentage, the `backend/coverage.xml` root line-rate, and whether the backend threshold is enforced by `fail_under`, `--cov-fail-under`, or both.
- Run `make phase7-proof-frontend` and record the final Jest statements, lines, functions, and branches percentages from the terminal output plus the computed line percentage from `frontend/coverage/lcov.info`.
- Run `make phase7-proof-e2e-list` only as a quick regression that the four Playwright specs are still discoverable. Do not add new Playwright specs, and do not make Docker E2E mandatory on push/pull_request.
- Create `.planning/phases/07-testing-stretch/07-05-SUMMARY.md` with sections named exactly `Backend Coverage Proof`, `Frontend Coverage Proof`, `Threshold Enforcement`, `Review Findings Closed`, and `TEST-04 Preservation`.
- If `07-VERIFICATION.md` is updated, keep the report factual and date-stamped. It must not claim verified completion unless the recorded proof commands actually pass.
  </action>
  <verify>
    <automated>cd /mnt/c/Users/dajcs/code/transcendence && make phase7-proof-backend && make phase7-proof-frontend && make phase7-proof-e2e-list</automated>
    <automated>cd /mnt/c/Users/dajcs/code/transcendence && rg -n "Backend Coverage Proof|Frontend Coverage Proof|Threshold Enforcement|Review Findings Closed|TEST-04 Preservation" .planning/phases/07-testing-stretch/07-05-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - `07-05-SUMMARY.md` contains `Backend Coverage Proof`
    - `07-05-SUMMARY.md` contains `Frontend Coverage Proof`
    - `07-05-SUMMARY.md` contains `Threshold Enforcement`
    - `07-05-SUMMARY.md` contains `Review Findings Closed`
    - `07-05-SUMMARY.md` contains `TEST-04 Preservation`
    - `make phase7-proof-e2e-list` still reports `Total: 4 tests in 4 files`
  </acceptance_criteria>
  <done>Phase 7 has a final gap-closure proof record for backend/frontend coverage while leaving the already verified E2E scope intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Coverage config -> CI status | Test commands determine whether CI can falsely pass below the required thresholds |
| Test harness -> production behavior | Mocked tests must prove current behavior without replacing product logic with permissive assertions |
| Coverage denominator -> requirement truth | Exclusions can hide product code and create misleading coverage percentages |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05-01 | R | `backend/pyproject.toml`, `frontend/jest.config.ts`, `Makefile` | mitigate | Add explicit backend and frontend fail-under gates so successful proof commands are auditable threshold evidence. |
| T-07-05-02 | T | Backend tests for market/comment rewards | mitigate | Replace permissive status-code-only assertions with balance/LP state assertions that fail on behavior regressions. |
| T-07-05-03 | I | Jest coverage configuration | mitigate | Keep product files in `collectCoverageFrom` and document any new exclusion so coverage cannot be inflated by hiding user-facing code. |
| T-07-05-04 | D | Full coverage proof runtime | accept | Run targeted stacks during task work and the full `make phase7-proof-backend` / `make phase7-proof-frontend` gates before completion; keep Docker E2E manual-only as already verified. |
</threat_model>

<verification>
- `cd /mnt/c/Users/dajcs/code/transcendence && make phase7-proof-backend`
- `cd /mnt/c/Users/dajcs/code/transcendence/backend && UV_CACHE_DIR="/mnt/c/Users/dajcs/code/transcendence/.cache/uv" uv run coverage report --include="app/services/economy_service.py" --fail-under=100`
- `cd /mnt/c/Users/dajcs/code/transcendence && make phase7-proof-frontend`
- `cd /mnt/c/Users/dajcs/code/transcendence && make phase7-proof-e2e-list`
- `cd /mnt/c/Users/dajcs/code/transcendence && rg -n "coverageThreshold|fail_under|cov-fail-under|Backend Coverage Proof|Frontend Coverage Proof" frontend/jest.config.ts backend/pyproject.toml Makefile .planning/phases/07-testing-stretch/07-05-SUMMARY.md`
</verification>

<success_criteria>
- Backend total coverage is at least 80 percent and `make phase7-proof-backend` fails below that threshold.
- Economy formula coverage is proven at 100 percent through `economy_service.py` full-file coverage or a documented narrower formula-only proof.
- Frontend Jest coverage is at least 70 percent for statements and lines and `make phase7-proof-frontend` fails below that threshold.
- The three warnings in `07-REVIEW.md` are closed with behavior assertions.
- No stretch modules, runner migrations, or expanded Playwright scope are introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/07-testing-stretch/07-05-SUMMARY.md`
</output>
