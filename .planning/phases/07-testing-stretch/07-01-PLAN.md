---
phase: "07"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/pyproject.toml
  - backend/tests/conftest.py
  - backend/tests/test_economy.py
  - backend/tests/test_resolution.py
  - backend/tests/test_auth.py
  - backend/tests/test_bets.py
  - backend/tests/test_users.py
autonomous: true
requirements: [TEST-01, TEST-02]
user_setup: []
must_haves:
  truths:
    - "Backend test tooling can run with coverage output in a CI-friendly way without replacing the existing pytest stack"
    - "Economy and payout edge cases have explicit regression coverage for the current LP/BP/TP formulas"
    - "Auth/session tests cover the cookie-based flow and current OAuth host/redirect behavior"
    - "Critical backend API flows for betting and user/profile behavior have concrete integration coverage beyond basic happy paths"
    - "The default backend test environment remains SQLite plus fakeredis, not a mandatory Postgres/Redis harness"
  artifacts:
    - path: "backend/pyproject.toml"
      provides: "Backend test and coverage command support"
      contains: "pytest"
    - path: "backend/tests/conftest.py"
      provides: "Default SQLite + fakeredis fixture baseline retained and refined if needed"
      contains: "sqlite+aiosqlite:///:memory:"
    - path: "backend/tests/test_economy.py"
      provides: "Economy formula regression coverage"
      contains: "compute"
    - path: "backend/tests/test_resolution.py"
      provides: "Resolution and payout regression coverage"
      contains: "test_"
    - path: "backend/tests/test_auth.py"
      provides: "Auth/session/OAuth regression coverage"
      contains: "test_"
  key_links:
    - from: "backend/pyproject.toml"
      to: "backend tests"
      via: "pytest coverage command path used by CI and local runs"
      pattern: "cov"
    - from: "backend/tests/conftest.py"
      to: "backend/tests/test_auth.py"
      via: "AsyncClient + dependency override + temp JWT keys"
      pattern: "client"
    - from: "backend/tests/test_resolution.py"
      to: "backend/app/services/resolution_service.py"
      via: "formula and payout regression assertions"
      pattern: "resolution_service"
---

<objective>
Create the backend half of the Phase 7 test baseline without changing the chosen harness. Add CI-friendly backend coverage support, then expand regression coverage in the highest-risk backend domains: economy formulas, resolution/payout logic, auth/session behavior, and critical API flows.

Purpose: TEST-01 and TEST-02 are best satisfied by strengthening the already-existing pytest suite rather than replacing it with a heavier environment.
Output: Updated backend test tooling plus expanded backend regression/integration tests grounded in the current SQLite + fakeredis fixture model.
</objective>

<execution_context>
@/mnt/c/Users/dajcs/code/transcendence/.codex/get-shit-done/workflows/execute-plan.md
@/mnt/c/Users/dajcs/code/transcendence/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-testing-stretch/07-CONTEXT.md
@.planning/phases/07-testing-stretch/07-RESEARCH.md
@backend/pyproject.toml
@backend/tests/conftest.py
@backend/tests/test_auth.py
@backend/tests/test_economy.py
@backend/tests/test_resolution.py
@backend/tests/test_bets.py
@backend/tests/test_users.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add backend coverage entrypoints without replacing the current harness</name>
  <files>backend/pyproject.toml, backend/tests/conftest.py</files>
  <read_first>backend/pyproject.toml, backend/tests/conftest.py, .planning/phases/07-testing-stretch/07-CONTEXT.md</read_first>
  <action>
Keep the existing pytest + pytest-asyncio + SQLite + fakeredis baseline intact, then add only the minimal tooling needed for CI-friendly backend coverage.

Specific changes:
- In `backend/pyproject.toml`, add `pytest-cov` to the backend dev dependency group. Do not introduce a new test runner or a Postgres-specific requirement.
- Add pytest configuration entries that make coverage collection predictable in CI. Use the existing `[tool.pytest.ini_options]` block and extend it with concrete defaults instead of creating a separate config file.
- Keep `testpaths = ["tests"]` intact.
- If fixture cleanup or singleton reset gaps would make broader backend coverage flaky, tighten `backend/tests/conftest.py` in-place using the current patterns. Do not replace the in-memory SQLite engine with a Docker-backed database.
- Do not add any fixture that makes a live Redis or Postgres server mandatory for the default suite.
  </action>
  <verify>cd /mnt/c/Users/dajcs/code/transcendence/backend && uv run pytest --cov=app --cov-report=term-missing tests/test_auth.py tests/test_economy.py tests/test_resolution.py -q</verify>
  <acceptance_criteria>
    - `backend/pyproject.toml` contains `pytest-cov` in the dev dependency group
    - `backend/pyproject.toml` still contains `pytest-asyncio` and preserves the pytest-based stack
    - `backend/tests/conftest.py` still uses `sqlite+aiosqlite:///:memory:` as the default DB engine
    - `backend/tests/conftest.py` still uses `FakeRedis` instead of requiring a live Redis service
    - No new backend default test path requires Docker services to run
  </acceptance_criteria>
  <done>Backend test tooling can emit CI-friendly coverage while preserving the current SQLite + fakeredis harness.</done>
</task>

<task type="auto">
  <name>Task 2: Expand deterministic formula and payout regression coverage</name>
  <files>backend/tests/test_economy.py, backend/tests/test_resolution.py</files>
  <read_first>backend/tests/test_economy.py, backend/tests/test_resolution.py, backend/app/services/resolution_service.py, backend/app/services/bet_service.py, .planning/phases/07-testing-stretch/07-RESEARCH.md</read_first>
  <action>
Strengthen the existing backend unit/regression coverage around the formulas and edge cases that are most likely to regress.

Specific coverage targets:
- Economy conversion and cap behavior from the current LP/BP rules
- Betting amount and refund edge cases where the user-visible economy changes
- Resolution payout behavior, including current binary/multichoice payout rules and numeric payout edge cases already codified in the app
- Guard-rail cases that previously changed during Phase 05 and 06.1, especially current TP and payout semantics

Prefer extending the existing `test_economy.py` and `test_resolution.py` files over creating a large new test taxonomy. Keep the assertions deterministic and direct. Do not fill the suite with placeholder xfails or implementation-only tests.
  </action>
  <verify>cd /mnt/c/Users/dajcs/code/transcendence/backend && uv run pytest tests/test_economy.py tests/test_resolution.py -q</verify>
  <acceptance_criteria>
    - `backend/tests/test_economy.py` contains additional concrete regression cases for current LP/BP economy behavior
    - `backend/tests/test_resolution.py` contains concrete payout or resolution edge-case coverage for the current app behavior
    - New tests assert observable current formulas, not deprecated pre-fix formulas
    - New tests do not rely on a live Postgres or Redis service
  </acceptance_criteria>
  <done>Current backend economy and payout logic have meaningful deterministic regression coverage that matches the live application rules.</done>
</task>

<task type="auto">
  <name>Task 3: Expand backend integration coverage for auth, bets, and user-facing API flows</name>
  <files>backend/tests/test_auth.py, backend/tests/test_bets.py, backend/tests/test_users.py</files>
  <read_first>backend/tests/test_auth.py, backend/tests/test_bets.py, backend/tests/test_users.py, backend/app/api/routes/auth.py, backend/app/api/routes/bets.py, backend/app/api/routes/users.py, .planning/phases/07-testing-stretch/07-CONTEXT.md</read_first>
  <action>
Use the existing `AsyncClient` integration style to broaden backend safety around critical flows instead of introducing a new integration harness.

Specific coverage targets:
- Auth/session: cookie behavior, refresh/logout/me-style flows, and current OAuth redirect-base or allowed-host behavior that already matters to the app
- Betting/API flows: at least one meaningful unhappy-path or state-transition case in addition to existing basic happy paths
- User/profile-related routes that influence visible product behavior and are cheap to exercise in the current test harness

Keep tests aligned with the current route contracts and current auth model (cookie-based auth). Do not write broad placeholder tests that only assert "not 404". Prefer meaningful status-code, payload, and behavior assertions.
  </action>
  <verify>cd /mnt/c/Users/dajcs/code/transcendence/backend && uv run pytest tests/test_auth.py tests/test_bets.py tests/test_users.py -q</verify>
  <acceptance_criteria>
    - `backend/tests/test_auth.py` covers current cookie/session behavior beyond basic registration and login
    - `backend/tests/test_bets.py` contains at least one stronger state or validation regression case than the current baseline
    - `backend/tests/test_users.py` contains user-facing API regression coverage relevant to current product behavior
    - Tests assert current route semantics and response bodies, not only route existence
  </acceptance_criteria>
  <done>Backend integration coverage protects the highest-risk auth and API flows using the existing in-process async test harness.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd backend && uv run pytest tests/test_economy.py tests/test_resolution.py tests/test_auth.py tests/test_bets.py tests/test_users.py -q`
- [ ] `cd backend && uv run pytest --cov=app --cov-report=term-missing tests/ -q`
- [ ] The default backend suite still runs without a live Postgres or Redis requirement
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Backend coverage tooling is CI-friendly without changing the chosen stack
- TEST-01 and TEST-02 are materially advanced by concrete backend tests, not just config changes
</success_criteria>

<output>
After completion, create `.planning/phases/07-testing-stretch/07-01-SUMMARY.md`
</output>
