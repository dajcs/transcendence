---
phase: "07"
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/jest.config.ts
  - frontend/src/components/auth/__tests__/LoginForm.test.tsx
  - frontend/src/__tests__/NotificationBell.test.tsx
  - frontend/src/components/nav/__tests__/TopNav.test.tsx
  - frontend/src/app/(protected)/markets/[id]/__tests__/market-detail.test.tsx
  - frontend/src/app/(protected)/profile/[username]/__tests__/profile-page.test.tsx
autonomous: true
requirements: [TEST-03]
user_setup: []
must_haves:
  truths:
    - "Frontend tests stay on Jest and React Testing Library; no Vitest migration is introduced"
    - "Frontend test commands can produce coverage output in a CI-friendly way"
    - "Critical UI regressions around login, notifications, navigation/balance display, market detail interactions, and profile rendering are covered by Jest tests"
    - "The expanded frontend suite follows current repo mocking patterns for API, routing, i18n, and Zustand stores"
  artifacts:
    - path: "frontend/package.json"
      provides: "Frontend test and coverage scripts"
      contains: "\"test\""
    - path: "frontend/jest.config.ts"
      provides: "Jest coverage configuration aligned with Next.js"
      contains: "testEnvironment"
    - path: "frontend/src/components/nav/__tests__/TopNav.test.tsx"
      provides: "Navigation and balance-display regression coverage"
      contains: "TopNav"
    - path: "frontend/src/app/(protected)/profile/[username]/__tests__/profile-page.test.tsx"
      provides: "Profile page regression coverage"
      contains: "profile"
    - path: "frontend/src/app/(protected)/markets/[id]/__tests__/market-detail.test.tsx"
      provides: "Market detail regression coverage"
      contains: "Market detail"
  key_links:
    - from: "frontend/package.json"
      to: "frontend/jest.config.ts"
      via: "coverage-capable Jest scripts"
      pattern: "coverage"
    - from: "frontend/src/components/auth/LoginForm.tsx"
      to: "frontend/src/components/auth/__tests__/LoginForm.test.tsx"
      via: "form validation and API error behavior assertions"
      pattern: "LoginForm"
    - from: "frontend/src/components/NotificationBell.tsx"
      to: "frontend/src/__tests__/NotificationBell.test.tsx"
      via: "socket-driven browser notification behavior"
      pattern: "NotificationBell"
---

<objective>
Build the frontend half of the Phase 7 regression baseline on top of the repo's current Jest setup. Add coverage-friendly command surfaces and expand tests around the most regression-prone user-facing UI flows without migrating frameworks.

Purpose: TEST-03 should be satisfied by strengthening the current Jest + RTL baseline, not by replacing it with Vitest.
Output: Updated frontend test scripts/config plus broader Jest regression coverage for key components and protected pages.
</objective>

<execution_context>
@/mnt/c/Users/dajcs/code/transcendence/.codex/get-shit-done/workflows/execute-plan.md
@/mnt/c/Users/dajcs/code/transcendence/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-testing-stretch/07-CONTEXT.md
@.planning/phases/07-testing-stretch/07-RESEARCH.md
@frontend/package.json
@frontend/jest.config.ts
@frontend/src/components/auth/__tests__/LoginForm.test.tsx
@frontend/src/__tests__/NotificationBell.test.tsx
@frontend/src/app/(protected)/markets/[id]/__tests__/market-detail.test.tsx
@frontend/src/components/nav/TopNav.tsx
@frontend/src/app/(protected)/profile/[username]/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add frontend Jest coverage entrypoints without changing runners</name>
  <files>frontend/package.json, frontend/jest.config.ts</files>
  <read_first>frontend/package.json, frontend/jest.config.ts, .planning/phases/07-testing-stretch/07-CONTEXT.md</read_first>
  <action>
Keep the current frontend runner (`jest` via `next/jest`) and add the minimum coverage-oriented command/config support needed for local and CI runs.

Specific changes:
- In `frontend/package.json`, keep the existing `test` script and add a concrete coverage-oriented script such as `test:coverage`. Do not swap the runner to Vitest.
- Extend `frontend/jest.config.ts` with explicit coverage collection settings suitable for the app structure. Keep the existing alias mapping and jsdom environment intact.
- If a small exclusion list is needed for generated or low-signal files, make it explicit in Jest config instead of leaving CI behavior implicit.
  </action>
  <verify>cd /mnt/c/Users/dajcs/code/transcendence/frontend && npm test -- --runInBand --coverage --watch=false</verify>
  <acceptance_criteria>
    - `frontend/package.json` still uses Jest for the test runner
    - `frontend/package.json` contains a coverage-capable test script
    - `frontend/jest.config.ts` contains explicit coverage configuration
    - No frontend test tooling migration to Vitest is introduced
  </acceptance_criteria>
  <done>Frontend Jest commands can run in local and CI contexts with explicit coverage output and without runner churn.</done>
</task>

<task type="auto">
  <name>Task 2: Expand auth, nav, and profile regression coverage</name>
  <files>frontend/src/components/auth/__tests__/LoginForm.test.tsx, frontend/src/components/nav/__tests__/TopNav.test.tsx, frontend/src/app/(protected)/profile/[username]/__tests__/profile-page.test.tsx</files>
  <read_first>frontend/src/components/auth/__tests__/LoginForm.test.tsx, frontend/src/components/auth/LoginForm.tsx, frontend/src/components/nav/TopNav.tsx, frontend/src/app/(protected)/profile/[username]/page.tsx, .planning/phases/07-testing-stretch/07-RESEARCH.md</read_first>
  <action>
Strengthen frontend regression tests around user-visible areas that are central to login, navigation state, and profile behavior.

Specific targets:
- Login form validation and API error handling beyond the current baseline
- Top navigation behavior that matters to current product state, especially balance rendering, auth-dependent controls, and key navigation affordances
- Profile page behavior that is currently visible in the app and susceptible to regression after recent phase changes

Follow the repo's existing mocking patterns:
- `jest.mock` for `@/lib/api`
- mock `next/navigation`
- mock `@/i18n`
- mock Zustand stores through selector wrappers

Prefer adding focused assertions to existing tests and adding one or two targeted new test files over creating a broad new test hierarchy.
  </action>
  <verify>cd /mnt/c/Users/dajcs/code/transcendence/frontend && npm test -- --runInBand frontend/src/components/auth/__tests__/LoginForm.test.tsx frontend/src/components/nav/__tests__/TopNav.test.tsx 'frontend/src/app/(protected)/profile/[username]/__tests__/profile-page.test.tsx'</verify>
  <acceptance_criteria>
    - `frontend/src/components/auth/__tests__/LoginForm.test.tsx` contains stronger validation or error-state coverage than the current baseline
    - `frontend/src/components/nav/__tests__/TopNav.test.tsx` exists and covers current nav behavior
    - `frontend/src/app/(protected)/profile/[username]/__tests__/profile-page.test.tsx` exists and covers current profile rendering or interaction behavior
    - Tests follow current Jest/mock patterns used elsewhere in the repo
  </acceptance_criteria>
  <done>Frontend auth, nav, and profile regressions are covered by concrete Jest tests that match current repo style.</done>
</task>

<task type="auto">
  <name>Task 3: Expand market-detail and notification regression coverage</name>
  <files>frontend/src/__tests__/NotificationBell.test.tsx, frontend/src/app/(protected)/markets/[id]/__tests__/market-detail.test.tsx</files>
  <read_first>frontend/src/__tests__/NotificationBell.test.tsx, frontend/src/components/NotificationBell.tsx, frontend/src/app/(protected)/markets/[id]/__tests__/market-detail.test.tsx, frontend/src/app/(protected)/markets/[id]/page.tsx</read_first>
  <action>
Broaden the UI regression net around two already-identified risk areas:
- socket-driven browser notification behavior
- market detail page behavior, especially query gating, interaction guards, and visible state-dependent UI

Use the existing tests as the base rather than rewriting them. Add concrete assertions for current behavior that would plausibly regress after product changes. Keep the suite behavior-focused and avoid snapshots.
  </action>
  <verify>cd /mnt/c/Users/dajcs/code/transcendence/frontend && npm test -- --runInBand frontend/src/__tests__/NotificationBell.test.tsx 'frontend/src/app/(protected)/markets/[id]/__tests__/market-detail.test.tsx'</verify>
  <acceptance_criteria>
    - `frontend/src/__tests__/NotificationBell.test.tsx` covers current browser-notification behavior with socket events
    - `frontend/src/app/(protected)/markets/[id]/__tests__/market-detail.test.tsx` contains additional meaningful market-detail regressions
    - New tests remain behavior-focused and do not use snapshot assertions
  </acceptance_criteria>
  <done>Frontend notification and market-detail regressions are protected by a broader Jest baseline.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd frontend && npm test -- --runInBand --watch=false`
- [ ] `cd frontend && npm test -- --runInBand --coverage --watch=false`
- [ ] Frontend tests still run under Jest with current alias and Next.js config behavior
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Frontend testing remains on the current Jest stack
- TEST-03 is materially advanced through real regression coverage, not just script changes
</success_criteria>

<output>
After completion, create `.planning/phases/07-testing-stretch/07-02-SUMMARY.md`
</output>
