---
context: phase
phase: 06.1-like-points-rename-economy-formula-fix
task: human_uat
status: awaiting_human_approval
last_updated: 2026-04-20T20:00:00Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

_No blocking constraints from this session._

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Stale `kp_` key references | Phase 6.1 renamed KP→LP but several callers still used `balances["kp"]`; fix committed in 2e329f8 | advisory | grep for `kp_` before marking any LP-rename phase done |

<current_state>
Phase 06.1 execution is complete (all 8 plans have SUMMARY.md). The phase is at the human UAT checkpoint — VERIFICATION.md is `human_needed`. Several UAT feedback items were fixed and committed (f86379e). The phase has NOT been marked complete in ROADMAP.md yet; that happens after the user approves human UAT.
</current_state>

<completed_work>

- Plans 01–08: all executed and committed
- Code review done: 4 warnings (WR-01 self-upvote, WR-02 race condition, WR-03 numeric TP, WR-04 dropdown floor), 5 info
- Verification done: 15/15 automated checks passed
- HUMAN-UAT.md created with 6 pending test items
- UAT feedback fixed (f86379e):
  - lp_converted notification: title "♥ Converted", body uses ♥ symbol, click → profile page
  - ♥→BP ledger row: description shows `<x> ♥` (LP derived from bp_delta)
  - No-event rows (bp_delta=0, tp_delta=0) filtered from ledger table
  - Profile tabs renamed to `<username>'s Points / Bets / Markets`
  - market_upvote LpEvents properly classified as lp_allocation in ledger service
  - i18n keys updated in en/fr/de
</completed_work>

<remaining_work>

- Human testing of 6 UAT items (see 06.1-HUMAN-UAT.md):
  1. Heart icon renders red in TopNav
  2. Market heart toggle (fill/unfill, server persistence)
  3. Comment heart toggle (fill/unfill, author LP decrement on unlike)
  4. Bet amount dropdown respects live balance
  5. Migration 015 preserves data against live DB
  6. LP→BP conversion cap at login (user with LP>1023 gets 10.0 BP)
- Once user approves: run update_roadmap step (mark phase complete, update ROADMAP.md, STATE.md, commit)
- Optionally: /gsd-code-review-fix 6.1 to address WR-02 (unlike race condition) and WR-03 (numeric TP broken)
</remaining_work>

<decisions_made>

- lp_allocation description: LP amount derived from bp_delta via `round(2^bp_delta - 1)` — avoids schema change
- No-event rows: filtered client-side (bp_delta===0 && tp_delta===0) — cleanest approach
- notify_lp_converted: added `username` param with default="" for backwards compat
</decisions_made>

<blockers>
- WR-03 (code review): `_compute_t_win` uses exact string match for numeric market winning_side — numeric winners get 0 TP. Advisory only, not blocking UAT approval.
</blockers>

## Required Reading (in order)
1. `.planning/phases/06.1-like-points-rename-economy-formula-fix/06.1-HUMAN-UAT.md` — 6 pending test items
2. `.planning/phases/06.1-like-points-rename-economy-formula-fix/06.1-REVIEW.md` — 4 warnings to consider

## Infrastructure State
- Docker: not running during this session (tests timed out without containers)
- Branch: `feat/ph6.1` — all changes committed, clean working tree

<context>
Session completed all execution, code review, and re-verification for phase 06.1. The only remaining step is human UAT approval followed by marking the phase complete. The UAT feedback from the user during this session was fully addressed and committed.
</context>

<next_action>
User tests the 6 items in 06.1-HUMAN-UAT.md with Docker running. On approval: run the `update_roadmap` step from execute-phase.md to mark phase 06.1 complete.
</next_action>
