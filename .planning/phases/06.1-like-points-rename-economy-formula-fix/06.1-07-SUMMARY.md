---
phase: 06.1-like-points-rename-economy-formula-fix
plan: "07"
subsystem: ui
tags: [nextjs, react, zustand, i18n, likes, upvotes]

requires:
  - phase: "06.1-01"
    provides: lp field in backend user response
  - phase: "06.1-04"
    provides: user_has_liked in MarketResponse/CommentResponse; unlike endpoints

provides:
  - User.lp replaces User.kp in Zustand auth store
  - TopNav heart display (❤️ N) desktop + mobile
  - Heart toggle for market and comment upvotes (❤️/♡ based on user_has_liked)
  - unlikeMarket + unlikeComment mutations wired to DELETE endpoints
  - Bet amount selector 1..min(10, floor(bp)) defaulting to 1
  - Dashboard and profile LP labels and user.lp/profile.lp usage
  - i18n KP→LP in en/de/fr (5 string pairs each)

affects: [frontend, dashboard, profile, markets]

tech-stack:
  added: []
  patterns: [persisted API state for liked toggle — no local marketLiked useState]

key-files:
  created: []
  modified:
    - frontend/src/store/auth.ts
    - frontend/src/lib/types.ts
    - frontend/src/components/nav/TopNav.tsx
    - frontend/src/app/(protected)/markets/[id]/page.tsx
    - frontend/src/app/(protected)/dashboard/page.tsx
    - frontend/src/app/(protected)/profile/[username]/page.tsx
    - frontend/src/i18n/en.ts
    - frontend/src/i18n/de.ts
    - frontend/src/i18n/fr.ts

key-decisions:
  - "Heart state comes from API (user_has_liked) not local useState — refresh-safe"
  - "Bet amount selector capped at min(10, floor(bp)) matching backend flat cap"

patterns-established:
  - "Unlike toggle: read user_has_liked from query data; invalidate on success"

requirements-completed: [ECON-01, ECON-02]

duration: 30min
completed: 2026-04-20
---

# Phase 06.1-07 Summary

**Frontend LP rename complete: heart toggle UI for markets/comments, bet amount selector, KP→LP across all i18n locales**

## Performance

- **Duration:** 30 min
- **Tasks:** 2
- **Files modified:** 9

## Accomplishments
- `User.kp` → `User.lp` in Zustand store and types.ts (Market + Comment interfaces get `user_has_liked`)
- TopNav shows `❤️ N` (red) in both desktop and mobile views
- Market upvote button: ❤️/♡ reads `market.user_has_liked` from API response; unlike wired to DELETE
- Comment upvote buttons: ❤️/♡ per comment from `comment.user_has_liked`; unlike mutation added
- Bet placement: `betAmount` state + dropdown 1..min(10, floor(bp)), sent as `amount` in POST
- Dashboard and profile pages use `user.lp` / `profile.lp` with LP labels
- 5 i18n string pairs updated in en/de/fr (KP→LP, Karma Points→Like Points)

## Task Commits

1. **Task 1: auth store, nav, dashboard, profile, i18n** - `5a33530` (feat)
2. **Task 2: heart toggle UI + bet amount selector** - `a13f7af` (feat)

## Deviations from Plan
None - plan executed as written.

## Issues Encountered
None

## Next Phase Readiness
- Plan 08 (docs update) can proceed

---
*Phase: 06.1-like-points-rename-economy-formula-fix*
*Completed: 2026-04-20*
