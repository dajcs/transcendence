---
phase: "06.1"
plan: "08"
type: execute
wave: 5
depends_on: ["06.1-03", "06.1-05", "06.1-06", "06.1-07"]
files_modified:
  - plan/ECONOMY.md
  - CLAUDE.md
  - README.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [ECON-01, ECON-02, ECON-03, ECON-04]
must_haves:
  truths:
    - "plan/ECONOMY.md describes Like Points (LP) not Karma Points (KP)"
    - "plan/ECONOMY.md reflects new LP→BP cap at 10.0, flat 10 BP bet cap, time-based TP, 10x payout cap, numeric window algorithm, removal of creator rebate"
    - "CLAUDE.md does not mention Karma Points"
    - ".planning/REQUIREMENTS.md defines ECON-01 through ECON-04"
    - ".planning/REQUIREMENTS.md BET-04, DISC-02, SOC-03, RES-06 reflect LP terminology and new formulas"
  artifacts:
    - path: "plan/ECONOMY.md"
      provides: "Updated economy spec with LP and all new formulas"
      contains: "Like Points (LP)"
    - path: ".planning/REQUIREMENTS.md"
      provides: "ECON-01 through ECON-04 requirement definitions; updated BET-04, DISC-02, SOC-03, RES-06"
      contains: "ECON-01"
---

<objective>
Update all documentation files to reflect the LP rename and new economy formulas. Define ECON-01 through ECON-04 requirements in REQUIREMENTS.md. Update BET-04, DISC-02, SOC-03, and RES-06 in REQUIREMENTS.md to remove stale KP/formula language. Update CLAUDE.md and README.md to remove "Karma Points" references.

Purpose: Documentation should match the implemented system. ECON-01..04 must be defined in REQUIREMENTS.md so checkers can validate the phase. The existing BET-04, DISC-02, SOC-03, RES-06 entries still use old KP/formula language — leaving them creates internal contradictions in the file.
Output: Updated plan/ECONOMY.md, CLAUDE.md, README.md, .planning/REQUIREMENTS.md.
</objective>

<execution_context>
@/mnt/c/Users/dajcs/code/transcendence/.claude/get-shit-done/workflows/execute-plan.md
@/mnt/c/Users/dajcs/code/transcendence/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06.1-like-points-rename-economy-formula-fix/06.1-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite plan/ECONOMY.md to reflect LP and all new formulas</name>
  <files>plan/ECONOMY.md</files>
  <read_first>
    - plan/ECONOMY.md (full file — understand all sections; every KP/Karma reference needs updating)
  </read_first>
  <action>
Rewrite `plan/ECONOMY.md` to replace Karma Points with Like Points and update all affected formula sections. Keep all other content (BP earning table header, TP formula intro, anti-gaming section) intact. Apply these changes:

1. **Header table**: `kp | Karma Points | Community contribution score` → `lp | Like Points | Community contribution score`

2. **Section heading**: `## Karma Points (kp)` → `## Like Points (lp)`

3. **Earning bullets**: `+1 kp per upvote...` → `+1 lp per upvote...`

4. **Type line**: `**kp** is an **integer**` → `**lp** is an **integer**`

5. **Edge cases**: Replace "kp" with "lp" throughout

6. **Purpose**: "Determines bp allocation on next login (see KP → BP Conversion)" → "Determines bp allocation on next login (see LP → BP Conversion)"

7. **LP → BP Conversion section**: Rename heading from `## KP → BP Conversion (at login)` to `## LP → BP Conversion (at login)`. Update the formula text:
   - Old: `karma_bp = log2(kp + 1)` → New: `karma_bp = min(log2(lp + 1), 10.0)` (capped at 10.0)
   - Update description: "Calculate `karma_bp = min(log2(lp + 1), 10.0)` — **full float, capped at 10 BP**"
   - Update examples table: add row `1023 | 10.0` and `2000 | 10.0 (capped)`
   - Update reset text: "Insert a lp event of `-lp_value`"
   - Update notification: "X LP converted to Y.y BP"

8. **BP Earning table** — two rows change:
   - `Daily login | +1.0 bp` stays
   - `KP conversion on login | +log2(kp + 1) bp (float)` → `LP conversion on login | +min(log2(lp + 1), 10.0) bp (float, capped at 10.0)`
   - `Voting on your own market | +1.0 bp (rebate — net cost is 0)` → **DELETE this row** (D-13 removes rebate)
   - `Winning a bet | proportional share of total pool (float)` → `Winning a bet | min(bet_amount × 10, proportional_share) BP; surplus to special fund`

9. **BET-04 / Bet cap section**: Update wherever bet cap formula appears. Change `floor(log2(kp+1)) + 1 bp` to `flat cap of 10 BP per position`.

10. **Truth Points section**: Update TP formula to remove floor:
    - `tp_earned = t_win / t_bet` (raw float, no floor) — update any mention of truncation

11. **Add new section after Truth Points**:
    ```
    ## Special Fund (bp_fund_entries)

    Surplus BP from capped payouts is recorded in the `bp_fund_entries` table.

    | Column | Type | Description |
    |--------|------|-------------|
    | market_id | UUID | FK to bets.id |
    | user_id | UUID | Winner whose payout was capped |
    | amount | Numeric | Surplus BP amount |
    | reason | Text | "cap_surplus" (binary/multichoice) or "numeric_surplus" |
    | created_at | DateTime | Event timestamp |

    Surplus use-case (lottery/distribution) is deferred to a later phase.
    ```

12. **Numeric market payout**: Add or update section describing window-expansion algorithm:
    - Exact match → 10x the bet amount
    - No exact match → expand window until ceil(total_bettors × 0.10) inside
    - Linear multiplier: rank 0 → 10x, rank N-1 → 0.1x; `multiplier = 10 - (rank / (window_size - 1)) × 9.9`
    - Surplus → special fund

13. Update footer `Last updated` to `2026-04-19`.
  </action>
  <verify>
    <automated>grep -n "Like Points\|Like Points (LP)\|lp.*converted\|min(log2\|cap.*10\|cap_surplus" /mnt/c/Users/dajcs/code/transcendence/plan/ECONOMY.md</automated>
  </verify>
  <acceptance_criteria>
    - plan/ECONOMY.md contains "Like Points (LP)" heading
    - plan/ECONOMY.md contains `min(log2(lp + 1), 10.0)` formula
    - plan/ECONOMY.md does NOT contain "Karma Points" or "Karma (KP)"
    - plan/ECONOMY.md does NOT contain "Voting on your own market" rebate row
    - plan/ECONOMY.md contains "Special Fund" section with "bp_fund_entries"
    - plan/ECONOMY.md does NOT contain `math.floor(raw * 100) / 100` or truncation in TP formula
    - plan/ECONOMY.md contains "flat cap of 10 BP" for bet cap
  </acceptance_criteria>
  <done>plan/ECONOMY.md fully describes the LP system with all D-08 through D-18 formulas.</done>
</task>

<task type="auto">
  <name>Task 2: Update CLAUDE.md, README.md, and define/update REQUIREMENTS.md entries</name>
  <files>
    CLAUDE.md,
    README.md,
    .planning/REQUIREMENTS.md
  </files>
  <read_first>
    - CLAUDE.md (full file — find "Karma Points" / "kp" / "KP" references in Project Overview section)
    - README.md (if it exists — find currency description lines)
    - .planning/REQUIREMENTS.md (full file — read all existing text for BET-04, DISC-02, SOC-03, RES-06 before editing; find Economy Extension section or v1 Requirements to add ECON block)
  </read_first>
  <action>
**`CLAUDE.md`:**
In the Project Overview section, find:
- "Users bet with platform-native points (Karma, Truth, Spice)" → "Users bet with platform-native points (Like Points, Betting Points, Truth Points)"
- Any mention of "kp" in the Architecture or Commands sections → rename to "lp"
- In the module targets or description: "reputation-based" stays; "Karma Points" or "KP" anywhere → "Like Points (LP)"

**`README.md`** (if file exists and mentions points):
- Replace "Karma Points" or "KP" with "Like Points (LP)"
- No structural changes to README otherwise

**`.planning/REQUIREMENTS.md` — update existing entries:**

Find and update these four existing requirement lines (confirmed current text from codebase):

- **BET-04** (current: `"Bet cap enforced per user per market: floor(log2(kp+1)) + 1 bp"`):
  Replace with: `"Bet cap: flat 10 BP per position (or available balance, whichever is lower). Backend enforces via _check_bet_cap(); UI dropdown limits options to 1..min(10, floor(balance))."`

- **DISC-02** (current: `"Users can upvote comments (earns kp for author)"`):
  Replace with: `"Users can upvote comments (earns lp for author); unlike removes upvote and decrements lp. Heart icon shows filled/empty based on user_has_liked."`

- **SOC-03** (current: `"User profile shows kp, tp, bet history, avatar"`):
  Replace with: `"User profile shows lp, tp, bet history, avatar"`

- **RES-06** (current: `"Winning bet pays proportional BP pool share + TP to each winner (D-11: floor(user_stake / total_winning_stake * total_bp_pool))"`):
  Replace with: `"Winning bet payout: min(bet_amount × 10, proportional_share) BP per winner; surplus goes to bp_fund_entries. TP formula: raw t_win / t_bet (no floor). Numeric markets use window-expansion algorithm (ceil(10% of bettors)) with linear multiplier 10x→0.1x by rank."`

**`.planning/REQUIREMENTS.md` — add new ECON section:**

Add a new section `### Economy (Phase 6.1)` after the existing v1 Requirements section (or within it), defining:

```markdown
### Economy (Phase 6.1)

- [ ] **ECON-01**: "Karma Points (KP)" renamed to "Like Points (LP)" in DB (lp_events table), backend Python code, API response fields, frontend Zustand store, and all i18n locale files
- [ ] **ECON-02**: UI displays LP as ❤️ N (red heart emoji + count) in nav; comment and market upvote buttons are heart icons (filled red when liked, empty otherwise); unlike removes upvote
- [ ] **ECON-03**: Economy formulas corrected: LP→BP conversion capped at 10.0 BP (`min(log2(lp+1), 10.0)`); bet cap flat 10 BP; TP formula is raw `t_win / t_bet` (no floor); binary/multichoice payout is `min(bet_amount × 10, proportional_share)` with surplus to bp_fund_entries; market creator rebate removed
- [ ] **ECON-04**: Numeric market payout uses window-expansion algorithm (ceil(10% of bettors)) with linear multiplier interpolation (10x→0.1x by rank); surplus goes to bp_fund_entries; bp_fund_entries table created (migration 016)
```

Also update the Traceability table at the bottom of REQUIREMENTS.md to add:
`| ECON-01 to ECON-04 | Phase 6.1 | Pending |`
  </action>
  <verify>
    <automated>grep -n "ECON-01\|ECON-02\|ECON-03\|ECON-04\|Like Points" /mnt/c/Users/dajcs/code/transcendence/.planning/REQUIREMENTS.md /mnt/c/Users/dajcs/code/transcendence/CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - .planning/REQUIREMENTS.md contains `**ECON-01**` through `**ECON-04**` definitions
    - .planning/REQUIREMENTS.md Traceability section contains "ECON-01 to ECON-04"
    - .planning/REQUIREMENTS.md BET-04 text contains "flat 10 BP" (not "floor(log2(kp+1))")
    - .planning/REQUIREMENTS.md DISC-02 text contains "lp" (not "kp") and "user_has_liked"
    - .planning/REQUIREMENTS.md SOC-03 text contains "lp" (not "kp")
    - .planning/REQUIREMENTS.md RES-06 text contains "t_win / t_bet" and "bp_fund_entries" (not "floor(user_stake")
    - CLAUDE.md does NOT contain "Karma Points" (replaced with "Like Points")
    - plan/ECONOMY.md last updated date is "2026-04-19"
  </acceptance_criteria>
  <done>All doc files updated; ECON-01..04 requirements defined and traceable; BET-04, DISC-02, SOC-03, RES-06 internally consistent with implemented formulas.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs | Documentation only; no runtime trust boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.1-18 | Info Disclosure | REQUIREMENTS.md | accept | Requirements file is internal planning doc; no sensitive data |
</threat_model>

<verification>
1. `grep -n "Karma Points" plan/ECONOMY.md CLAUDE.md` — must return no results
2. `grep -n "ECON-0[1-4]" .planning/REQUIREMENTS.md` — must return 4 lines (one per requirement)
3. `grep -n "Like Points" plan/ECONOMY.md` — must match
4. `grep -n "min(log2" plan/ECONOMY.md` — must match LP→BP formula
5. `grep -n "Special Fund\|bp_fund_entries" plan/ECONOMY.md` — must match
6. `grep -n "flat 10 BP" .planning/REQUIREMENTS.md` — must match BET-04
7. `grep -n "t_win / t_bet\|bp_fund_entries" .planning/REQUIREMENTS.md` — must match RES-06
8. `grep -n "user_has_liked" .planning/REQUIREMENTS.md` — must match DISC-02
9. `grep -n "floor(log2(kp\|kp.*bet cap\|floor(user_stake" .planning/REQUIREMENTS.md` — must return no results
</verification>

<success_criteria>
- plan/ECONOMY.md reflects the full LP economy with all D-08 through D-18 changes
- ECON-01 through ECON-04 defined in .planning/REQUIREMENTS.md
- CLAUDE.md and README.md no longer mention "Karma Points"
- BET-04, DISC-02, SOC-03, RES-06 in REQUIREMENTS.md updated to LP terminology and new formulas — file is internally consistent
- Documentation is self-consistent with implemented code
</success_criteria>

<output>
After completion, create `.planning/phases/06.1-like-points-rename-economy-formula-fix/06.1-08-SUMMARY.md`
</output>
