---
phase: 06.1-like-points-rename-economy-formula-fix
plan: "06"
subsystem: api
tags: [fastapi, sqlalchemy, resolution, payout, numeric, economy]

requires:
  - phase: "06.1-05"
    provides: trigger_payout with market_type branching and BpFundEntry import

provides:
  - _compute_numeric_payouts() — pure function implementing D-14/D-15/D-16/D-17
  - Numeric branch in trigger_payout — replaces proportional placeholder
  - BpFundEntry(reason='numeric_surplus') — surplus tracking for numeric markets
  - Exact-match path, window-expansion path, edge case window_size=1

affects: [resolution_service, economics]

tech-stack:
  added: []
  patterns: [window-expansion with sorted distance, linear multiplier interpolation]

key-files:
  created: []
  modified:
    - backend/app/services/resolution_service.py

key-decisions:
  - "_compute_numeric_payouts is pure (no DB) — testable in isolation; DB fetch happens in trigger_payout"
  - "Surplus keyed to proposer_id as custodian — BpFundEntry market_id links it to the market"
  - "math.ceil ensures at least 1 winner even with very small markets"

patterns-established:
  - "Window = top ceil(N*0.10) by |prediction - resolution|; rank 0 closest"
  - "multiplier = 10 - (rank/(window_size-1)) * 9.9; edge: window_size==1 → multiplier=10"

requirements-completed: [ECON-03, ECON-04]

duration: 15min
completed: 2026-04-20
---

# Phase 06.1-06 Summary

**Numeric market payout algorithm: window-expansion + linear interpolation with surplus tracked in BpFundEntry**

## Performance

- **Duration:** 15 min
- **Started:** 2026-04-20T10:00:00Z
- **Completed:** 2026-04-20T10:15:00Z
- **Tasks:** 2
- **Files modified:** 1

## Accomplishments
- Added pure helper `_compute_numeric_payouts(positions, resolution_value)` implementing D-14–D-17
- Exact-match path: winners get 10× their stake; surplus → pool
- No-exact-match: window expands to `ceil(N*0.10)` bettors sorted by distance; rank 0 gets 10×, rank N-1 gets 0.1×
- Edge case: `window_size == 1` → multiplier = 10.0 (full 10×)
- Wired into `trigger_payout` as `if market_type == "numeric":` block; binary/multichoice path preserved intact
- Surplus tracked as `BpFundEntry(reason='numeric_surplus')`

## Task Commits

1. **Task 1+2: _compute_numeric_payouts + trigger_payout wiring** - `89ef09f` (feat)

## Files Created/Modified
- `backend/app/services/resolution_service.py` — _compute_numeric_payouts added; trigger_payout numeric branch wired

## Decisions Made
- Helper is pure Python (no async/DB) for testability; DB fetch of all_positions happens in trigger_payout before calling it
- Surplus attributed to proposer_id as custodian in BpFundEntry (market_id provides the link)

## Deviations from Plan
None - plan executed exactly as written.

## Issues Encountered
None

## Next Phase Readiness
- Plan 08 (docs) can reference the complete payout algorithm

---
*Phase: 06.1-like-points-rename-economy-formula-fix*
*Completed: 2026-04-20*
