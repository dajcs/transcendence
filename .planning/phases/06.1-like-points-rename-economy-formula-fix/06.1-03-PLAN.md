---
phase: "06.1"
plan: "03"
type: execute
wave: 2
depends_on: ["06.1-01"]
files_modified:
  - backend/app/services/economy_service.py
  - backend/app/services/bet_service.py
  - backend/app/services/comment_service.py
  - backend/app/services/market_service.py
  - backend/app/services/auth_service.py
  - backend/app/services/oauth_service.py
  - backend/app/services/gdpr_service.py
  - backend/app/services/ledger_service.py
  - backend/app/services/notification_service.py
  - backend/app/db/models/__init__.py
  - backend/tests/test_economy.py
  - backend/tests/test_socket.py
  - backend/tests/test_tasks.py
  - backend/scripts/seed_dev.py
autonomous: true
requirements: [ECON-01, ECON-02]
must_haves:
  truths:
    - "get_balance() returns {bp, lp, tp} — not kp"
    - "convert_kp_to_bp() renamed to convert_lp_to_bp(); uses LpEvent; caps at 10.0 BP"
    - "compute_bet_cap() removed; _check_bet_cap() uses flat 10 BP cap"
    - "Market creator rebate (+1 BP own_bet_vote) removed from place_bet()"
    - "comment_service and market_service import LpEvent (not KpEvent)"
    - "auth_service, oauth_service call convert_lp_to_bp() (not convert_kp_to_bp)"
    - "gdpr_service, ledger_service, models/__init__.py use LpEvent (not KpEvent)"
    - "notification_service.py has notify_lp_converted (not notify_kp_converted)"
    - "test_economy.py tests convert_lp_to_bp cap formula; test_socket and test_tasks import LpEvent"
    - "seed_dev.py uses LpEvent (not KpEvent) at all 3 insert sites"
  artifacts:
    - path: "backend/app/services/economy_service.py"
      provides: "get_balance with lp key; convert_lp_to_bp with cap; no compute_bet_cap"
      contains: "convert_lp_to_bp"
    - path: "backend/app/services/bet_service.py"
      provides: "flat 10 BP cap; no rebate; no KpEvent import"
      contains: "amount > 10"
    - path: "backend/app/services/auth_service.py"
      provides: "calls convert_lp_to_bp on login"
      contains: "convert_lp_to_bp"
    - path: "backend/tests/test_economy.py"
      provides: "tests for convert_lp_to_bp cap"
      contains: "convert_lp_to_bp"
  key_links:
    - from: "backend/app/services/economy_service.py"
      to: "LpEvent"
      via: "from app.db.models.transaction import LpEvent"
      pattern: "LpEvent"
    - from: "backend/app/services/bet_service.py"
      to: "economy_service"
      via: "compute_bet_cap removed from imports"
      pattern: "amount > 10"
    - from: "backend/app/services/auth_service.py"
      to: "economy_service.convert_lp_to_bp"
      via: "from app.services.economy_service import ... convert_lp_to_bp"
      pattern: "convert_lp_to_bp"
---

<objective>
Update all backend service files that reference KpEvent to use LpEvent. Apply the three economy formula fixes: (1) LP→BP conversion cap at 10.0, (2) flat 10 BP bet cap replacing the log10 formula, (3) remove market creator rebate (+1 BP). Also update all caller services (auth, oauth, gdpr, ledger, notification, models/__init__), test files, and seed script that still reference KpEvent or convert_kp_to_bp.

Purpose: Closes D-08, D-09, D-10, D-13 formula decisions. Service-level renames complete the KP→LP rename chain started in Plan 01. All import sites are covered — no post-execution ImportError.
Output: Updated economy_service.py, bet_service.py, comment_service.py, market_service.py, auth_service.py, oauth_service.py, gdpr_service.py, ledger_service.py, notification_service.py, models/__init__.py, test_economy.py, test_socket.py, test_tasks.py, seed_dev.py.
</objective>

<execution_context>
@/mnt/c/Users/dajcs/code/transcendence/.claude/get-shit-done/workflows/execute-plan.md
@/mnt/c/Users/dajcs/code/transcendence/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.1-like-points-rename-economy-formula-fix/06.1-CONTEXT.md
@.planning/phases/06.1-like-points-rename-economy-formula-fix/06.1-PATTERNS.md

<interfaces>
<!-- economy_service.py: functions to change -->
<!-- compute_bet_cap (lines 15-21) — DELETE entire function -->
<!-- get_balance (lines 45-63) — rename kp_result/kp → lp_result/lp; return "lp" key; use LpEvent -->
<!-- convert_kp_to_bp (lines 112-127) — rename to convert_lp_to_bp; add min(...,10.0) cap; use LpEvent -->

<!-- bet_service.py: changes needed -->
<!-- line 12: remove KpEvent from imports -->
<!-- line 21: remove compute_bet_cap from imports -->
<!-- lines 73-89 (_check_bet_cap): replace body with flat cap; remove KpEvent query; remove compute_bet_cap call -->
<!-- line 114-115: remove proposer rebate (own_bet_vote credit_bp call) -->

<!-- comment_service.py: line 258 KpEvent → LpEvent -->
<!-- market_service.py: line 300-314 KpEvent → LpEvent in upvote_market() -->

<!-- auth_service.py (confirmed): -->
<!-- line 12: from app.services.economy_service import credit_bp, convert_kp_to_bp -->
<!-- line 109: kp_converted, bp_earned = await convert_kp_to_bp(db, user.id) -->
<!-- line 112: from app.services.notification_service import notify_kp_converted -->
<!-- line 113: await notify_kp_converted(db, user.id, kp_converted, bp_earned) -->

<!-- oauth_service.py (confirmed): same pattern as auth_service.py -->
<!-- line 16: from app.services.economy_service import credit_bp, convert_kp_to_bp -->
<!-- line 163: kp_converted, bp_earned = await convert_kp_to_bp(db, user.id) -->
<!-- line 166-167: notify_kp_converted call -->

<!-- gdpr_service.py (confirmed): -->
<!-- line 13: from app.db.models.transaction import BpTransaction, KpEvent, TpTransaction -->
<!-- line 60: rows = (await db.execute(select(KpEvent).where(KpEvent.user_id == uid))).scalars().all() -->
<!-- line 61: kp_events = [{"amount": r.amount, ...} for r in rows] -->
<!-- line 90: "kp_events": kp_events, -->
<!-- line 116: await db.execute(delete(KpEvent).where(KpEvent.user_id == uid)) -->

<!-- ledger_service.py (confirmed): -->
<!-- line 10: from app.db.models.transaction import BpTransaction, KpEvent, TpTransaction -->
<!-- line 47-54: KpEvent query for ledger union_all -->
<!-- line 82-85: reason mapping from "kp_conversion" / "comment_upvote" → "kp_allocation" -->

<!-- notification_service.py: -->
<!-- async def notify_kp_converted( → async def notify_lp_converted( -->
<!-- notification_type parameter value "kp_converted" → "lp_converted" -->
<!-- message template containing "KP converted" → "LP converted" -->

<!-- models/__init__.py (confirmed): -->
<!-- line 4: from app.db.models.transaction import BpTransaction, KpEvent, TpTransaction  # noqa: F401 -->

<!-- test_economy.py: imports compute_bet_cap from economy_service (lines 5-12) -->
<!-- test_socket.py line 72: from app.db.models.transaction import KpEvent, BpTransaction -->
<!-- test_tasks.py line 25: from app.db.models.transaction import BpTransaction, KpEvent -->
<!-- seed_dev.py line 28: from app.db.models.transaction import BpTransaction, KpEvent -->
<!-- seed_dev.py lines 383, 492, 543: KpEvent insert sites -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: economy_service.py — rename, cap, remove bet-cap function</name>
  <files>backend/app/services/economy_service.py</files>
  <read_first>
    - backend/app/services/economy_service.py (full file — understand all KpEvent usages and function signatures)
    - backend/app/db/models/transaction.py (confirm LpEvent is the class name after Plan 01)
  </read_first>
  <action>
Apply these changes to `backend/app/services/economy_service.py`:

1. **Import line** (currently line 11): Change `from app.db.models.transaction import BpTransaction, KpEvent, TpTransaction` to `from app.db.models.transaction import BpTransaction, LpEvent, TpTransaction`

2. **Delete `compute_bet_cap()` function entirely** (currently lines 15-21 — the log10 formula). Remove the function and its docstring. It is replaced by a flat constant in bet_service.py.

3. **`get_balance()` function** (currently lines 45-63): Rename all KP-related variables and return key:
   - `kp_result = await db.execute(select(func.sum(KpEvent.amount))...)` → `lp_result = await db.execute(select(func.sum(LpEvent.amount))...)`
   - `kp = int(kp_result.scalar_one() or 0)` → `lp = int(lp_result.scalar_one() or 0)`
   - return `{"bp": bp, "kp": kp, "tp": tp}` → `{"bp": bp, "lp": lp, "tp": tp}`
   - Update docstring comment from "kp" to "lp"

4. **`convert_kp_to_bp()` function** (currently lines 112-127): Rename to `convert_lp_to_bp()` and fix:
   - Rename function: `async def convert_lp_to_bp(db: AsyncSession, user_id: uuid.UUID) -> tuple[int, float]:`
   - Update docstring: "Convert accumulated LP to BP using min(log2(lp+1), 10.0). Resets LP to 0."
   - Rename `kp_total` → `lp_total`: `lp_total = (await db.execute(select(func.sum(LpEvent.amount)).where(LpEvent.user_id == user_id))).scalar_one()`
   - Rename `kp_value` → `lp_value`: `lp_value = int(lp_total or 0)`
   - Guard: `if lp_value <= 0: return 0, 0.0`
   - **Add cap**: `karma_bp = min(math.log2(lp_value + 1), 10.0)` (was `karma_bp = math.log2(kp_value + 1)`)
   - BpTransaction reason: `reason="lp_conversion"` (was `"kp_conversion"`)
   - Reset insert: `db.add(LpEvent(user_id=user_id, amount=-lp_value, source_type="lp_reset_login", source_id=user_id, day_date=today))` (was KpEvent with kp_reset_login)
   - Return: `return lp_value, karma_bp` (was `kp_value, karma_bp`)

5. Update any remaining references in the file's docstring or comments from "kp" to "lp".
  </action>
  <verify>
    <automated>grep -n "compute_bet_cap\|KpEvent\|\"kp\"\|kp_value\|kp_total\|convert_kp\|kp_reset" /mnt/c/Users/dajcs/code/transcendence/backend/app/services/economy_service.py</automated>
  </verify>
  <acceptance_criteria>
    - economy_service.py contains `def convert_lp_to_bp(` (not convert_kp_to_bp)
    - economy_service.py contains `min(math.log2(lp_value + 1), 10.0)` (cap present)
    - economy_service.py contains `LpEvent` (not KpEvent)
    - economy_service.py contains `"lp": lp` in get_balance return dict
    - economy_service.py does NOT contain `compute_bet_cap` function definition
    - economy_service.py does NOT contain `KpEvent`
    - economy_service.py does NOT contain `"kp": kp` in get_balance return dict
  </acceptance_criteria>
  <done>economy_service.py uses LpEvent throughout; get_balance returns lp key; convert_lp_to_bp caps at 10 BP; compute_bet_cap deleted.</done>
</task>

<task type="auto">
  <name>Task 2: bet_service, comment_service, market_service — rename refs and fix bet cap</name>
  <files>
    backend/app/services/bet_service.py,
    backend/app/services/comment_service.py,
    backend/app/services/market_service.py
  </files>
  <read_first>
    - backend/app/services/bet_service.py (full file — understand _check_bet_cap, place_bet proposer rebate at line 114-115)
    - backend/app/services/comment_service.py (lines 100-135 — upvote_comment KpEvent usage)
    - backend/app/services/market_service.py (lines 305-330 — upvote_market KpEvent usage)
  </read_first>
  <action>
**`backend/app/services/bet_service.py`:**

1. Remove `KpEvent` from the imports at line 12: change `from app.db.models.transaction import KpEvent` — remove this import entirely (KpEvent no longer used here).

2. Remove `compute_bet_cap` from the economy_service import at line 21: change `from app.services.economy_service import (compute_bet_cap, compute_numeric_refund_bp, ...)` — remove `compute_bet_cap` from the list.

3. Replace `_check_bet_cap()` body (currently lines 73-89) with flat 10 BP cap. New implementation:
```python
async def _check_bet_cap(db: AsyncSession, user_id: uuid.UUID, amount: float) -> None:
    """BET-04: flat cap of 10 BP per position (D-09)."""
    if amount > 10:
        raise HTTPException(
            status_code=422,
            detail=f"Bet amount {amount} exceeds the maximum of 10 bp per position.",
        )
```
Remove the `today`, `kp` query, and `compute_bet_cap` call. Remove the `datetime` import from `datetime` if it's only used in _check_bet_cap (check other usages first).

4. Remove proposer rebate: at `place_bet()`, find and delete these two lines (currently around line 114-115):
```python
    if market.proposer_id == user_id:
        await credit_bp(db, user_id=user_id, amount=data.amount, reason="own_bet_vote", bet_id=data.bet_id)
```
Market creators pay 1 BP like any other participant (D-13). Do NOT remove the `deduct_bp` call — just the conditional rebate.

**`backend/app/services/comment_service.py`:**
- In `upvote_comment()`, change `from app.db.models.transaction import KpEvent` (or top-level import) to `LpEvent`
- Change `KpEvent(user_id=comment.user_id, amount=1, source_type="comment_upvote", ...)` to `LpEvent(...)`

**`backend/app/services/market_service.py`:**
- In `upvote_market()`, change `from app.db.models.transaction import KpEvent` to `LpEvent`
- Change `KpEvent(user_id=market.proposer_id, amount=1, source_type="market_upvote", ...)` to `LpEvent(...)`
  </action>
  <verify>
    <automated>grep -n "KpEvent\|compute_bet_cap\|own_bet_vote" /mnt/c/Users/dajcs/code/transcendence/backend/app/services/bet_service.py /mnt/c/Users/dajcs/code/transcendence/backend/app/services/comment_service.py /mnt/c/Users/dajcs/code/transcendence/backend/app/services/market_service.py</automated>
  </verify>
  <acceptance_criteria>
    - bet_service.py does NOT contain `KpEvent`
    - bet_service.py does NOT contain `compute_bet_cap`
    - bet_service.py does NOT contain `own_bet_vote`
    - bet_service.py `_check_bet_cap` body contains `amount > 10`
    - comment_service.py contains `LpEvent` (not KpEvent)
    - market_service.py contains `LpEvent` (not KpEvent)
  </acceptance_criteria>
  <done>All service files use LpEvent; _check_bet_cap enforces flat 10 BP; proposer rebate removed.</done>
</task>

<task type="auto">
  <name>Task 3: Caller services, notification service, models __init__, test files, seed script — rename KpEvent/convert_kp_to_bp</name>
  <files>
    backend/app/services/auth_service.py,
    backend/app/services/oauth_service.py,
    backend/app/services/gdpr_service.py,
    backend/app/services/ledger_service.py,
    backend/app/services/notification_service.py,
    backend/app/db/models/__init__.py,
    backend/tests/test_economy.py,
    backend/tests/test_socket.py,
    backend/tests/test_tasks.py,
    backend/scripts/seed_dev.py
  </files>
  <read_first>
    - backend/app/services/auth_service.py (full file — find all convert_kp_to_bp and notify_kp_converted calls)
    - backend/app/services/oauth_service.py (full file — same pattern as auth_service)
    - backend/app/services/gdpr_service.py (full file — KpEvent import, select, delete, and key name)
    - backend/app/services/ledger_service.py (full file — KpEvent query in union_all; reason string "kp_allocation")
    - backend/app/services/notification_service.py (full file — find notify_kp_converted function and all KP references)
    - backend/app/db/models/__init__.py (full file — re-export line)
    - backend/tests/test_economy.py (full file — compute_bet_cap import and test cases)
    - backend/tests/test_socket.py (lines 70-90 — KpEvent import and usage)
    - backend/tests/test_tasks.py (lines 20-40 — KpEvent import and usage)
    - backend/scripts/seed_dev.py (lines 25-35 for import; lines 378-395, 488-498, 538-548 for insert sites)
  </read_first>
  <action>
**`backend/app/services/auth_service.py`:**
1. Line 12: change `from app.services.economy_service import credit_bp, convert_kp_to_bp` → `from app.services.economy_service import credit_bp, convert_lp_to_bp`
2. Line 109: change `kp_converted, bp_earned = await convert_kp_to_bp(db, user.id)` → `lp_converted, bp_earned = await convert_lp_to_bp(db, user.id)`
3. Line 112: change `from app.services.notification_service import notify_kp_converted` → `from app.services.notification_service import notify_lp_converted`
4. Line 113: change `await notify_kp_converted(db, user.id, kp_converted, bp_earned)` → `await notify_lp_converted(db, user.id, lp_converted, bp_earned)`

**`backend/app/services/oauth_service.py`:**
Apply the identical four changes as auth_service.py above (same import pattern at line 16, same call pattern at lines 163, 166-167).

**`backend/app/services/notification_service.py`:**
1. Rename function: `async def notify_kp_converted(` → `async def notify_lp_converted(`
2. Rename `notification_type` parameter value string: `"kp_converted"` → `"lp_converted"`
3. Rename message template: replace any string containing `"KP converted"` → `"LP converted"`

**`backend/app/services/gdpr_service.py`:**
1. Line 13: change `from app.db.models.transaction import BpTransaction, KpEvent, TpTransaction` → `from app.db.models.transaction import BpTransaction, LpEvent, TpTransaction`
2. Line 60: change `select(KpEvent).where(KpEvent.user_id == uid)` → `select(LpEvent).where(LpEvent.user_id == uid)`
3. Line 61: change the list comprehension variable name from `kp_events` to `lp_events` (update all 3 references: list comp, the dict key at line 90, and the delete at line 116)
4. Line 90: change `"kp_events": kp_events` → `"lp_events": lp_events`
5. Line 116: change `delete(KpEvent).where(KpEvent.user_id == uid)` → `delete(LpEvent).where(LpEvent.user_id == uid)`

**`backend/app/services/ledger_service.py`:**
1. Line 10: change `from app.db.models.transaction import BpTransaction, KpEvent, TpTransaction` → `from app.db.models.transaction import BpTransaction, LpEvent, TpTransaction`
2. Lines 47-54: change the `kp_q` query — replace all `KpEvent` references with `LpEvent`. Rename the variable `kp_q` → `lp_q`. Update the `union_all(bp_q, tp_q, kp_q)` call → `union_all(bp_q, tp_q, lp_q)`.
3. Lines 82-85: update reason string mappings — change `"kp_conversion"` → `"lp_conversion"` and `"kp_allocation"` → `"lp_allocation"` in the case/when expressions.

**`backend/app/db/models/__init__.py`:**
Line 4: change `from app.db.models.transaction import BpTransaction, KpEvent, TpTransaction  # noqa: F401` → `from app.db.models.transaction import BpTransaction, LpEvent, TpTransaction  # noqa: F401`

**`backend/tests/test_economy.py`:**
Replace the entire `test_compute_bet_cap_formula` test function with a test for `convert_lp_to_bp` cap behavior. The new test is synchronous (no DB needed — test the pure math boundary). Replace the function:

```python
def test_convert_lp_to_bp_cap():
    """D-08: LP→BP uses min(log2(lp+1), 10.0). Cap enforced at 10.0 BP."""
    import math

    def _formula(lp: int) -> float:
        return min(math.log2(lp + 1), 10.0)

    assert _formula(0) == 0.0
    assert _formula(1) == 1.0
    assert abs(_formula(1023) - 10.0) < 0.001
    assert _formula(2000) == 10.0
```

Remove the `from app.services.economy_service import compute_bet_cap` import line entirely (lines 5-12). Keep any other test functions in the file unchanged.

**`backend/tests/test_socket.py`:**
Line 72: change `from app.db.models.transaction import KpEvent, BpTransaction` → `from app.db.models.transaction import LpEvent, BpTransaction`
Line 86: change `kp_event = KpEvent(user_id=user_id, ...)` → `kp_event = LpEvent(user_id=user_id, ...)` (keep variable name `kp_event` to avoid wider refactor, or rename to `lp_event` — either is acceptable as long as it refers to LpEvent class).

**`backend/scripts/seed_dev.py`:**
Line 28: change `from app.db.models.transaction import BpTransaction, KpEvent` → `from app.db.models.transaction import BpTransaction, LpEvent`
Lines 383, 492, 543 (the three KpEvent insert sites): change `KpEvent(` → `LpEvent(` at each site. The constructor arguments (user_id, amount, source_type, source_id, day_date) are unchanged.
  </action>
  <verify>
    <automated>grep -rn "KpEvent\|convert_kp_to_bp\|compute_bet_cap\|notify_kp_converted" /mnt/c/Users/dajcs/code/transcendence/backend/app/services/auth_service.py /mnt/c/Users/dajcs/code/transcendence/backend/app/services/oauth_service.py /mnt/c/Users/dajcs/code/transcendence/backend/app/services/gdpr_service.py /mnt/c/Users/dajcs/code/transcendence/backend/app/services/ledger_service.py /mnt/c/Users/dajcs/code/transcendence/backend/app/services/notification_service.py /mnt/c/Users/dajcs/code/transcendence/backend/app/db/models/__init__.py /mnt/c/Users/dajcs/code/transcendence/backend/tests/test_economy.py /mnt/c/Users/dajcs/code/transcendence/backend/tests/test_socket.py /mnt/c/Users/dajcs/code/transcendence/backend/tests/test_tasks.py /mnt/c/Users/dajcs/code/transcendence/backend/scripts/seed_dev.py</automated>
  </verify>
  <acceptance_criteria>
    - auth_service.py and oauth_service.py contain `convert_lp_to_bp` (not convert_kp_to_bp)
    - auth_service.py and oauth_service.py contain `notify_lp_converted` (not notify_kp_converted)
    - notification_service.py contains `async def notify_lp_converted(` (not notify_kp_converted)
    - notification_service.py contains `"lp_converted"` as notification_type value (not "kp_converted")
    - notification_service.py contains `"LP converted"` in message template (not "KP converted")
    - gdpr_service.py contains `LpEvent` (not KpEvent); contains `"lp_events"` key (not "kp_events")
    - ledger_service.py contains `LpEvent` (not KpEvent); reason strings use "lp_conversion" and "lp_allocation"
    - models/__init__.py re-exports `LpEvent` (not KpEvent)
    - test_economy.py contains `test_convert_lp_to_bp_cap`; does NOT contain `compute_bet_cap`
    - test_socket.py contains `LpEvent` (not KpEvent)
    - test_tasks.py contains `LpEvent` (not KpEvent)
    - seed_dev.py contains `LpEvent` (not KpEvent) at all 4 sites (import + 3 inserts)
  </acceptance_criteria>
  <done>All import and call sites for KpEvent/convert_kp_to_bp/notify_kp_converted updated; pytest will not fail with ImportError; seed script works after rename.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→bet API | Bet amount from client request is validated here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.1-05 | Elevation | _check_bet_cap | mitigate | Flat 10 BP cap enforced server-side with 422 response; client cannot bypass via any amount > 10 |
| T-06.1-06 | Tampering | rebate removal | accept | Removing credit means net cost is always -1 BP for proposer; no new risk introduced |
</threat_model>

<verification>
1. `grep -rn "KpEvent" backend/app/services/ backend/app/db/models/__init__.py backend/tests/ backend/scripts/` — must return no results
2. `grep -rn "convert_kp_to_bp\|compute_bet_cap" backend/app/services/ backend/tests/` — must return no results
3. `grep -n "own_bet_vote" backend/app/services/bet_service.py` — must return no results
4. `grep -n "amount > 10" backend/app/services/bet_service.py` — must match
5. `grep -n "min(math.log2" backend/app/services/economy_service.py` — must match with 10.0 cap
6. `grep -n "test_convert_lp_to_bp_cap" backend/tests/test_economy.py` — must match
7. `grep -n "notify_kp_converted" backend/app/services/notification_service.py backend/app/services/auth_service.py backend/app/services/oauth_service.py` — must return no results
8. `grep -n "notify_lp_converted" backend/app/services/notification_service.py` — must match function definition
</verification>

<success_criteria>
- economy_service.py: convert_lp_to_bp() with min(log2(lp+1), 10.0) cap; get_balance returns "lp" key
- bet_service.py: _check_bet_cap is flat 10 BP; no KpEvent import; no proposer rebate
- comment_service.py and market_service.py: LpEvent used in upvote functions
- auth_service.py, oauth_service.py: call convert_lp_to_bp and notify_lp_converted (login LP→BP conversion works)
- notification_service.py: notify_lp_converted function; "lp_converted" type; "LP converted" message
- gdpr_service.py: selects and deletes LpEvent rows; exports "lp_events" key
- ledger_service.py: queries LpEvent; uses "lp_allocation" reason strings
- models/__init__.py: re-exports LpEvent
- test_economy.py: tests convert_lp_to_bp cap with 4 cases (0, 1, 1023, 2000)
- test_socket.py, test_tasks.py: import LpEvent — no ImportError at pytest collection
- seed_dev.py: inserts LpEvent at all 3 sites — seed script runs after rename
</success_criteria>

<output>
After completion, create `.planning/phases/06.1-like-points-rename-economy-formula-fix/06.1-03-SUMMARY.md`
</output>
