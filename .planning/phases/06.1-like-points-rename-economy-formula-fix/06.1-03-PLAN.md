---
phase: "06.1"
plan: "03"
type: execute
wave: 2
depends_on: ["06.1-01"]
files_modified:
  - backend/app/services/economy_service.py
  - backend/app/services/bet_service.py
  - backend/app/services/comment_service.py
  - backend/app/services/market_service.py
autonomous: true
requirements: [ECON-01, ECON-02]
must_haves:
  truths:
    - "get_balance() returns {bp, lp, tp} — not kp"
    - "convert_kp_to_bp() renamed to convert_lp_to_bp(); uses LpEvent; caps at 10.0 BP"
    - "compute_bet_cap() removed; _check_bet_cap() uses flat 10 BP cap"
    - "Market creator rebate (+1 BP own_bet_vote) removed from place_bet()"
    - "comment_service and market_service import LpEvent (not KpEvent)"
  artifacts:
    - path: "backend/app/services/economy_service.py"
      provides: "get_balance with lp key; convert_lp_to_bp with cap; no compute_bet_cap"
      contains: "convert_lp_to_bp"
    - path: "backend/app/services/bet_service.py"
      provides: "flat 10 BP cap; no rebate; no KpEvent import"
      contains: "amount > 10"
  key_links:
    - from: "backend/app/services/economy_service.py"
      to: "LpEvent"
      via: "from app.db.models.transaction import LpEvent"
      pattern: "LpEvent"
    - from: "backend/app/services/bet_service.py"
      to: "economy_service"
      via: "compute_bet_cap removed from imports"
      pattern: "amount > 10"
---

<objective>
Update all backend service files that reference KpEvent to use LpEvent. Apply the three economy formula fixes: (1) LP→BP conversion cap at 10.0, (2) flat 10 BP bet cap replacing the log10 formula, (3) remove market creator rebate (+1 BP). These are pure Python changes — no new DB schema needed.

Purpose: Closes D-08, D-09, D-10, D-13 formula decisions. Service-level renames complete the KP→LP rename chain started in Plan 01.
Output: Updated economy_service.py, bet_service.py, comment_service.py, market_service.py.
</objective>

<execution_context>
@/mnt/c/Users/dajcs/code/transcendence/.claude/get-shit-done/workflows/execute-plan.md
@/mnt/c/Users/dajcs/code/transcendence/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.1-like-points-rename-economy-formula-fix/06.1-CONTEXT.md
@.planning/phases/06.1-like-points-rename-economy-formula-fix/06.1-PATTERNS.md

<interfaces>
<!-- economy_service.py: functions to change -->
<!-- compute_bet_cap (lines 15-21) — DELETE entire function -->
<!-- get_balance (lines 45-63) — rename kp_result/kp → lp_result/lp; return "lp" key; use LpEvent -->
<!-- convert_kp_to_bp (lines 112-127) — rename to convert_lp_to_bp; add min(...,10.0) cap; use LpEvent -->

<!-- bet_service.py: changes needed -->
<!-- line 12: remove KpEvent from imports -->
<!-- line 21: remove compute_bet_cap from imports -->
<!-- lines 73-89 (_check_bet_cap): replace body with flat cap; remove KpEvent query; remove compute_bet_cap call -->
<!-- line 114-115: remove proposer rebate (own_bet_vote credit_bp call) -->

<!-- comment_service.py: line 258 KpEvent → LpEvent -->
<!-- market_service.py: line 300-314 KpEvent → LpEvent in upvote_market() -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: economy_service.py — rename, cap, remove bet-cap function</name>
  <files>backend/app/services/economy_service.py</files>
  <read_first>
    - backend/app/services/economy_service.py (full file — understand all KpEvent usages and function signatures)
    - backend/app/db/models/transaction.py (confirm LpEvent is the class name after Plan 01)
  </read_first>
  <action>
Apply these changes to `backend/app/services/economy_service.py`:

1. **Import line** (currently line 11): Change `from app.db.models.transaction import BpTransaction, KpEvent, TpTransaction` to `from app.db.models.transaction import BpTransaction, LpEvent, TpTransaction`

2. **Delete `compute_bet_cap()` function entirely** (currently lines 15-21 — the log10 formula). Remove the function and its docstring. It is replaced by a flat constant in bet_service.py.

3. **`get_balance()` function** (currently lines 45-63): Rename all KP-related variables and return key:
   - `kp_result = await db.execute(select(func.sum(KpEvent.amount))...)` → `lp_result = await db.execute(select(func.sum(LpEvent.amount))...)`
   - `kp = int(kp_result.scalar_one() or 0)` → `lp = int(lp_result.scalar_one() or 0)`
   - return `{"bp": bp, "kp": kp, "tp": tp}` → `{"bp": bp, "lp": lp, "tp": tp}`
   - Update docstring comment from "kp" to "lp"

4. **`convert_kp_to_bp()` function** (currently lines 112-127): Rename to `convert_lp_to_bp()` and fix:
   - Rename function: `async def convert_lp_to_bp(db: AsyncSession, user_id: uuid.UUID) -> tuple[int, float]:`
   - Update docstring: "Convert accumulated LP to BP using min(log2(lp+1), 10.0). Resets LP to 0."
   - Rename `kp_total` → `lp_total`: `lp_total = (await db.execute(select(func.sum(LpEvent.amount)).where(LpEvent.user_id == user_id))).scalar_one()`
   - Rename `kp_value` → `lp_value`: `lp_value = int(lp_total or 0)`
   - Guard: `if lp_value <= 0: return 0, 0.0`
   - **Add cap**: `karma_bp = min(math.log2(lp_value + 1), 10.0)` (was `karma_bp = math.log2(kp_value + 1)`)
   - BpTransaction reason: `reason="lp_conversion"` (was `"kp_conversion"`)
   - Reset insert: `db.add(LpEvent(user_id=user_id, amount=-lp_value, source_type="lp_reset_login", source_id=user_id, day_date=today))` (was KpEvent with kp_reset_login)
   - Return: `return lp_value, karma_bp` (was `kp_value, karma_bp`)

5. Update any remaining references in the file's docstring or comments from "kp" to "lp".
  </action>
  <verify>
    <automated>grep -n "compute_bet_cap\|KpEvent\|\"kp\"\|kp_value\|kp_total\|convert_kp\|kp_reset" /mnt/c/Users/dajcs/code/transcendence/backend/app/services/economy_service.py</automated>
  </verify>
  <acceptance_criteria>
    - economy_service.py contains `def convert_lp_to_bp(` (not convert_kp_to_bp)
    - economy_service.py contains `min(math.log2(lp_value + 1), 10.0)` (cap present)
    - economy_service.py contains `LpEvent` (not KpEvent)
    - economy_service.py contains `"lp": lp` in get_balance return dict
    - economy_service.py does NOT contain `compute_bet_cap` function definition
    - economy_service.py does NOT contain `KpEvent`
    - economy_service.py does NOT contain `"kp": kp` in get_balance return dict
  </acceptance_criteria>
  <done>economy_service.py uses LpEvent throughout; get_balance returns lp key; convert_lp_to_bp caps at 10 BP; compute_bet_cap deleted.</done>
</task>

<task type="auto">
  <name>Task 2: bet_service, comment_service, market_service — rename refs and fix bet cap</name>
  <files>
    backend/app/services/bet_service.py,
    backend/app/services/comment_service.py,
    backend/app/services/market_service.py
  </files>
  <read_first>
    - backend/app/services/bet_service.py (full file — understand _check_bet_cap, place_bet proposer rebate at line 114-115)
    - backend/app/services/comment_service.py (lines 100-135 — upvote_comment KpEvent usage)
    - backend/app/services/market_service.py (lines 305-330 — upvote_market KpEvent usage)
  </read_first>
  <action>
**`backend/app/services/bet_service.py`:**

1. Remove `KpEvent` from the imports at line 12: change `from app.db.models.transaction import KpEvent` — remove this import entirely (KpEvent no longer used here).

2. Remove `compute_bet_cap` from the economy_service import at line 21: change `from app.services.economy_service import (compute_bet_cap, compute_numeric_refund_bp, ...)` — remove `compute_bet_cap` from the list.

3. Replace `_check_bet_cap()` body (currently lines 73-89) with flat 10 BP cap. New implementation:
```python
async def _check_bet_cap(db: AsyncSession, user_id: uuid.UUID, amount: float) -> None:
    """BET-04: flat cap of 10 BP per position (D-09)."""
    if amount > 10:
        raise HTTPException(
            status_code=422,
            detail=f"Bet amount {amount} exceeds the maximum of 10 bp per position.",
        )
```
Remove the `today`, `kp` query, and `compute_bet_cap` call. Remove the `datetime` import from `datetime` if it's only used in _check_bet_cap (check other usages first).

4. Remove proposer rebate: at `place_bet()`, find and delete these two lines (currently around line 114-115):
```python
    if market.proposer_id == user_id:
        await credit_bp(db, user_id=user_id, amount=data.amount, reason="own_bet_vote", bet_id=data.bet_id)
```
Market creators pay 1 BP like any other participant (D-13). Do NOT remove the `deduct_bp` call — just the conditional rebate.

**`backend/app/services/comment_service.py`:**
- In `upvote_comment()`, change `from app.db.models.transaction import KpEvent` (or top-level import) to `LpEvent`
- Change `KpEvent(user_id=comment.user_id, amount=1, source_type="comment_upvote", ...)` to `LpEvent(...)`

**`backend/app/services/market_service.py`:**
- In `upvote_market()`, change `from app.db.models.transaction import KpEvent` to `LpEvent`
- Change `KpEvent(user_id=market.proposer_id, amount=1, source_type="market_upvote", ...)` to `LpEvent(...)`
  </action>
  <verify>
    <automated>grep -n "KpEvent\|compute_bet_cap\|own_bet_vote" /mnt/c/Users/dajcs/code/transcendence/backend/app/services/bet_service.py /mnt/c/Users/dajcs/code/transcendence/backend/app/services/comment_service.py /mnt/c/Users/dajcs/code/transcendence/backend/app/services/market_service.py</automated>
  </verify>
  <acceptance_criteria>
    - bet_service.py does NOT contain `KpEvent`
    - bet_service.py does NOT contain `compute_bet_cap`
    - bet_service.py does NOT contain `own_bet_vote`
    - bet_service.py `_check_bet_cap` body contains `amount > 10`
    - comment_service.py contains `LpEvent` (not KpEvent)
    - market_service.py contains `LpEvent` (not KpEvent)
  </acceptance_criteria>
  <done>All service files use LpEvent; _check_bet_cap enforces flat 10 BP; proposer rebate removed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→bet API | Bet amount from client request is validated here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.1-05 | Elevation | _check_bet_cap | mitigate | Flat 10 BP cap enforced server-side with 422 response; client cannot bypass via any amount > 10 |
| T-06.1-06 | Tampering | rebate removal | accept | Removing credit means net cost is always -1 BP for proposer; no new risk introduced |
</threat_model>

<verification>
1. `grep -rn "KpEvent" backend/app/services/` — must return no results
2. `grep -n "compute_bet_cap" backend/app/services/` — must return no results
3. `grep -n "own_bet_vote" backend/app/services/bet_service.py` — must return no results
4. `grep -n "amount > 10" backend/app/services/bet_service.py` — must match
5. `grep -n "min(math.log2" backend/app/services/economy_service.py` — must match with 10.0 cap
</verification>

<success_criteria>
- economy_service.py: convert_lp_to_bp() with min(log2(lp+1), 10.0) cap; get_balance returns "lp" key
- bet_service.py: _check_bet_cap is flat 10 BP; no KpEvent import; no proposer rebate
- comment_service.py and market_service.py: LpEvent used in upvote functions
</success_criteria>

<output>
After completion, create `.planning/phases/06.1-like-points-rename-economy-formula-fix/06.1-03-SUMMARY.md`
</output>
