---
phase: 06.1-like-points-rename-economy-formula-fix
plan: "01"
subsystem: database
tags: [alembic, sqlalchemy, postgresql, pydantic, migration, rename]

requires:
  - phase: 05.1-uat-gaps
    provides: KpEvent model and kp_events table established as the LP ledger

provides:
  - Alembic migration 015 renaming kp_events table to lp_events (preserves all rows)
  - LpEvent SQLAlchemy model with __tablename__ = 'lp_events'
  - UserResponse schema returning lp field instead of kp
  - PublicProfileResponse schema returning lp field instead of kp

affects:
  - 06.1-02 (frontend LP rename depends on lp field in API responses)
  - 06.1-03 (service layer KpEvent→LpEvent import update)
  - 06.1-04 (economy formula fix reads from lp_events via LpEvent)

tech-stack:
  added: []
  patterns:
    - "op.rename_table() for transactional DDL table renames in PostgreSQL via Alembic"
    - "ALTER SEQUENCE IF EXISTS as safe no-op for UUID-PK tables with no sequences"

key-files:
  created:
    - backend/alembic/versions/015_rename_kp_to_lp.py
  modified:
    - backend/app/db/models/transaction.py
    - backend/app/schemas/auth.py
    - backend/app/schemas/profile.py

key-decisions:
  - "ALTER SEQUENCE IF EXISTS is safe for UUID PKs — UUIDs have no auto-increment sequence, so the statement is a no-op without error"
  - "Service files importing KpEvent are left unchanged in this plan — handled in Plan 03 to keep each plan's scope minimal"

requirements-completed: [ECON-01]

duration: 8min
completed: 2026-04-20
---

# Phase 06.1 Plan 01: KP→LP Foundation — Migration and Schema Rename Summary

**Alembic migration 015 renames kp_events table to lp_events via op.rename_table(); LpEvent model and lp API schema fields replace KpEvent/kp throughout the data layer foundation**

## Performance

- **Duration:** ~8 min
- **Started:** 2026-04-20T00:00:00Z
- **Completed:** 2026-04-20T00:08:00Z
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments

- Created Alembic migration 015 with correct revision chain (015 revises 014), renames kp_events→lp_events transactionally in both upgrade() and downgrade()
- Renamed Python model class KpEvent→LpEvent with __tablename__ updated to 'lp_events'
- Renamed kp field to lp in UserResponse (auth.py) and PublicProfileResponse (profile.py)
- Zero schema drift: all column definitions preserved identically; only identifiers changed

## Task Commits

Each task was committed atomically:

1. **Task 1: Alembic migration 015 — rename kp_events to lp_events** - `f5bc763` (feat)
2. **Task 2: Rename KpEvent→LpEvent model; update auth and profile schemas** - `7d62e50` (feat)

## Files Created/Modified

- `backend/alembic/versions/015_rename_kp_to_lp.py` - Migration 015: op.rename_table kp_events→lp_events with safe sequence rename
- `backend/app/db/models/transaction.py` - KpEvent→LpEvent class rename, tablename kp_events→lp_events, docstring updated
- `backend/app/schemas/auth.py` - UserResponse.kp renamed to UserResponse.lp
- `backend/app/schemas/profile.py` - PublicProfileResponse.kp renamed to PublicProfileResponse.lp

## Decisions Made

- ALTER SEQUENCE IF EXISTS used in migration — safe no-op when UUID primary keys have no auto-increment sequence. Avoids migration failure on any environment where sequence names differ.
- Service layer imports (KpEvent in kp_service.py, economy_service.py etc.) deliberately left unchanged in this plan; handled in Plan 03 to keep scope minimal and commits atomic.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required. Migration runs automatically at backend container start via Alembic in the entrypoint.

## Next Phase Readiness

- Migration 015 and LpEvent model provide the database and ORM foundation required by Plans 02, 03, and 04
- UserResponse.lp and PublicProfileResponse.lp are the API contract that frontend Plan 02 will consume
- Service layer files still reference KpEvent — Plan 03 will update those imports

---
*Phase: 06.1-like-points-rename-economy-formula-fix*
*Completed: 2026-04-20*
