# Phase 6.1: Like Points Rename + Economy Formula Fix - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-19
**Phase:** 06.1-like-points-rename-economy-formula-fix
**Areas discussed:** KP→LP rename depth, TP formula, Requirements vs code discrepancy (expanded into full economy formula audit)

---

## KP → LP Rename Depth

| Option | Description | Selected |
|--------|-------------|----------|
| Full rename | DB table + Python class + API field + frontend store + i18n + docs | ✓ |
| Shallow rename | Keep DB table, rename everything above it | |
| Display-only | Only update i18n translation strings | |

**User's choice:** Full rename at all layers  
**Notes:** User also specified that docs (plan/ECONOMY.md, CLAUDE.md, README) should be updated.

---

## Heart UI (emerged from rename discussion)

| Question | Options | Selected |
|----------|---------|----------|
| Nav bar format | ♥ N vs N ♥ | ♥ N (heart first) |
| Like state persistence | Persisted per user vs local UI toggle | Persisted per user |
| Heart color/animation | Red no animation vs red with pop animation | Red, no animation |
| LP balance behavior | Same as KP (resets on login) vs changed | Same as KP |

**Notes:** User described Twitter-style heart toggle. Empty heart = not liked, filled heart = liked, clicking fills/unfills and increments/decrements count.

---

## TP Formula — Time vs Stake

| Option | Description | Selected |
|--------|-------------|----------|
| Time-based: tp = t_win / t_bet | Fix code to match ECONOMY.md spec | ✓ |
| Stake-proportional: tp = bp_staked / total_winning_stake | Keep current code, update spec | |
| Hybrid | Keep both functions, let Claude decide wiring | |

**User's choice:** Fix code to time-based formula  
**Notes:** User chose raw float (no truncation floor(raw*100)/100 removed).

---

## Economy Formula Audit (expanded from Requirements vs code discrepancy)

### LP → BP Conversion Cap
**User specified:**  
- Formula: `min(log2(lp+1), 10.0)` — float, capped at 10 BP
- Current code missing the 10 BP cap

### Bet Cap — Flat 10 BP
**User specified:**  
- Remove log10 formula entirely
- New rule: flat 10 BP cap per position
- UI: default 1 BP, dropdown for 1..min(10, balance)

| Question | Options | Selected |
|----------|---------|----------|
| Dropdown type | Discrete options 1..min(10, balance) vs free-text input | Discrete options |

### Market Creator Rebate
**User specified:**  
- Revoked — no more +1 BP rebate for betting on own market

### Binary/Multichoice Payout — 10x Cap
**User specified:**  
- Old proportional rule replaced with max payout = bet_amount × 10
- Surplus to special fund

### Numeric Market Payout — New Formula
**User specified:**  
- Exact match → 10x multiplier
- No exact match → expand window until 10% of all bettors are in window
- Linear multiplier: 10x (closest) to 0.1x (farthest in window)
- Surplus to special fund

### Special Fund Tracking
| Option | Description | Selected |
|--------|-------------|----------|
| Track in DB now | New table per event per user | ✓ |
| Log only | Just log the surplus amount | |

**Notes:** User wants to track market_id, user_id, amount per surplus entry. Use-case (lottery etc.) deferred.

---

## Claude's Discretion

- Heart icon implementation (emoji vs SVG)
- Alembic migration strategy (alter vs create-new)
- Numeric window-expansion algorithm edge cases

## Deferred Ideas

- Special fund usage (lottery or other distribution) — later phase
