---
phase: 06.1-like-points-rename-economy-formula-fix
plan: "04"
subsystem: api
tags: [fastapi, sqlalchemy, likes, upvotes, lp]

requires:
  - phase: "06.1-01"
    provides: LP/KP rename in models and DB
  - phase: "06.1-03"
    provides: LpEvent model and unlike LP decrement pattern

provides:
  - DELETE /api/comments/{id}/upvote — removes upvote, decrements author LP by 1
  - DELETE /api/markets/{id}/upvote — removes upvote, decrements proposer LP by 1
  - CommentResponse.user_has_liked — per-user like state in comment list
  - MarketResponse.user_has_liked — per-user like state in market detail
  - _get_current_user_optional helper in both routes — optional auth for anonymous reads

affects: [06.1-07, frontend]

tech-stack:
  added: []
  patterns: [optional-auth cookie extraction for read endpoints, batch liked_ids query for list endpoints]

key-files:
  created: []
  modified:
    - backend/app/schemas/comment.py
    - backend/app/schemas/market.py
    - backend/app/services/comment_service.py
    - backend/app/services/market_service.py
    - backend/app/api/routes/comments.py
    - backend/app/api/routes/markets.py

key-decisions:
  - "Optional auth uses cookie read with HTTPException swallowed — unauthenticated always gets user_has_liked=False"
  - "Batch liked_ids query in list_comments avoids N+1 queries"
  - "Unlike is idempotent — no-op if upvote row doesn't exist"

patterns-established:
  - "_get_current_user_optional: cookie→token→user, returns None on any failure"
  - "Batch set lookup for user_has_liked in list endpoints"

requirements-completed: [ECON-01]

duration: 15min
completed: 2026-04-20
---

# Phase 06.1-04 Summary

**Unlike endpoints (DELETE /upvote) and user_has_liked flag added to comments and markets API**

## Performance

- **Duration:** 15 min
- **Started:** 2026-04-20T09:15:00Z
- **Completed:** 2026-04-20T09:30:00Z
- **Tasks:** 2
- **Files modified:** 6

## Accomplishments
- Added `user_has_liked: bool = False` to CommentResponse and MarketResponse schemas
- Added `unlike_comment()` and `unlike_market()` service functions — delete upvote row and insert LpEvent(amount=-1)
- Updated `list_comments()` and `get_market()` to accept `current_user_id` and compute `user_has_liked`
- Added `_get_current_user_optional` helper in both routes files for anonymous-safe reads
- Added DELETE `/comments/{id}/upvote` and DELETE `/{market_id}/upvote` endpoints

## Task Commits

1. **Task 1: unlike functions + user_has_liked in service/schema layer** - `d652623` (feat)
2. **Task 2: DELETE endpoints + optional-auth wiring** - included in `d652623` (feat)

## Files Created/Modified
- `backend/app/schemas/comment.py` — user_has_liked field added to CommentResponse
- `backend/app/schemas/market.py` — user_has_liked field added to MarketResponse
- `backend/app/services/comment_service.py` — unlike_comment(), list_comments with current_user_id
- `backend/app/services/market_service.py` — unlike_market(), get_market with current_user_id
- `backend/app/api/routes/comments.py` — DELETE endpoint, _get_current_user_optional, updated list_comments route
- `backend/app/api/routes/markets.py` — DELETE endpoint, _get_current_user_optional, updated get_market route

## Decisions Made
- Unlike operations are idempotent (no-op if not upvoted) — prevents 409 errors from double clicks
- Batch set query for liked_ids avoids N+1 on comment lists
- Optional auth returns None on any failure (no 401 for public reads)

## Deviations from Plan
None - plan executed exactly as written.

## Issues Encountered
None

## Next Phase Readiness
- Frontend (Plan 07) can now use `user_has_liked` to render ❤️/♡ toggle and call DELETE on unlike

---
*Phase: 06.1-like-points-rename-economy-formula-fix*
*Completed: 2026-04-20*
