---
phase: 06.1-like-points-rename-economy-formula-fix
plan: "05"
subsystem: api
tags: [fastapi, sqlalchemy, resolution, payout, tp, bp, economy]

requires:
  - phase: "06.1-02"
    provides: BpFundEntry model and migration 016

provides:
  - compute_tp_earned() — raw float t_win/t_bet, no floor truncation
  - _compute_tp_for_user() — time-based TP via _compute_t_win + compute_tp_earned
  - trigger_payout() — 10x BP cap per winner for binary/multichoice; surplus → BpFundEntry(reason='cap_surplus')
  - t_bet and market_type computation in trigger_payout
  - Placeholder numeric branch preserved for Plan 06

affects: [06.1-06, resolution_service]

tech-stack:
  added: []
  patterns: [10x payout cap with surplus tracking, time-based TP formula]

key-files:
  created: []
  modified:
    - backend/app/services/resolution_service.py

key-decisions:
  - "10x cap per winner stake (not pool); surplus = proportional_share - capped_amount goes to BpFundEntry"
  - "TP is raw float ratio (t_win/t_bet) — no floor, more accurate"
  - "Numeric branch left as proportional placeholder until Plan 06 replaces it"

patterns-established:
  - "BpFundEntry inserted in same db.begin() transaction as payout — atomic surplus tracking"
  - "deadline_aware and t_bet computed once per payout, passed to per-user TP function"

requirements-completed: [ECON-03]

duration: 20min
completed: 2026-04-20
---

# Phase 06.1-05 Summary

**TP formula fixed to time-based raw float; binary/multichoice payout capped at 10x stake with surplus tracked in BpFundEntry**

## Performance

- **Duration:** 20 min
- **Started:** 2026-04-20T09:30:00Z
- **Completed:** 2026-04-20T09:50:00Z
- **Tasks:** 2
- **Files modified:** 1

## Accomplishments
- `compute_tp_earned()` now returns `t_win / t_bet` with no `math.floor` truncation
- `_compute_tp_for_user()` rewritten to use `_compute_t_win()` + `compute_tp_earned()` (time-based, no stake-proportional formula)
- `trigger_payout()` computes `t_bet` and `market_type` before winner loop
- Winner loop for binary/multichoice: `winner_bp = min(bet_amount * 10, proportional_share)`; surplus → `BpFundEntry(reason='cap_surplus')`
- Proposer penalty section preserved unchanged

## Task Commits

1. **Task 1+2: TP formula fix + trigger_payout 10x cap** - `7b2c683` (feat)

## Files Created/Modified
- `backend/app/services/resolution_service.py` — compute_tp_earned fixed; _compute_tp_for_user time-based; trigger_payout with t_bet/market_type/10x cap/BpFundEntry surplus

## Decisions Made
- `BpFundEntry` imports were already in place from Plan 02 — no additional import needed
- Numeric branch kept as proportional placeholder (Plan 06 will replace it)
- All BpFundEntry inserts within the existing `async with db.begin()` block for atomicity

## Deviations from Plan
None - plan executed exactly as written.

## Issues Encountered
None

## Next Phase Readiness
- Plan 06 can now replace the numeric branch in trigger_payout with the window expansion algorithm
- Plan 08 documentation can reference the correct TP and BP payout formulas

---
*Phase: 06.1-like-points-rename-economy-formula-fix*
*Completed: 2026-04-20*
