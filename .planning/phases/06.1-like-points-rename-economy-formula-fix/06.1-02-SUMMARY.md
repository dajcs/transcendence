---
phase: "06.1"
plan: "02"
subsystem: "backend/economy"
tags: [alembic, sqlalchemy, bp_fund_entries, economy, ledger]
dependency_graph:
  requires: ["06.1-01"]
  provides: ["BpFundEntry model", "migration 016"]
  affects: ["backend/app/db/models/transaction.py", "backend/alembic/versions/"]
tech_stack:
  added: []
  patterns: ["insert-only ledger pattern (BpFundEntry follows BpTransaction shape)"]
key_files:
  created:
    - backend/alembic/versions/016_add_bp_fund_entries.py
  modified:
    - backend/app/db/models/transaction.py
decisions:
  - "BpFundEntry follows same insert-only ledger pattern as BpTransaction; no UPDATE/DELETE endpoints"
  - "market_id FK references bets.id (existing table name); matches plan interface spec"
metrics:
  duration: "5 minutes"
  completed: "2026-04-20"
  tasks_completed: 2
  tasks_total: 2
  files_created: 1
  files_modified: 1
---

# Phase 06.1 Plan 02: BpFundEntry Model and Migration 016 Summary

BpFundEntry insert-only ledger model and Alembic migration 016 creating bp_fund_entries table for surplus BP tracking from capped payouts.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Alembic migration 016 — create bp_fund_entries table | 181a65e | backend/alembic/versions/016_add_bp_fund_entries.py |
| 2 | BpFundEntry model in transaction.py | 419c06f | backend/app/db/models/transaction.py |

## What Was Built

**Migration 016** (`backend/alembic/versions/016_add_bp_fund_entries.py`):
- Creates `bp_fund_entries` table with columns: id (UUID PK), market_id (FK bets.id), user_id (FK users.id), amount (Numeric 10,2), reason (Text), created_at (DateTime TZ)
- `down_revision = "015"` — chains after KP→LP rename migration
- `downgrade()` drops the table

**BpFundEntry model** (`backend/app/db/models/transaction.py`):
- Class appended after LpEvent; follows identical ledger shape as BpTransaction
- All required SQLAlchemy imports were already present in the file
- `__tablename__ = "bp_fund_entries"` matches migration DDL exactly

## Deviations from Plan

None - plan executed exactly as written.

## Known Stubs

None — this plan creates infrastructure (model + migration) for use by Plans 05/06. No UI or data flow involved.

## Threat Flags

None — no new network endpoints, auth paths, or trust boundary crossings introduced. Table access is insert-only as per threat register acceptance (T-06.1-03, T-06.1-04).

## Self-Check: PASSED

- [x] `backend/alembic/versions/016_add_bp_fund_entries.py` exists
- [x] `backend/app/db/models/transaction.py` contains `class BpFundEntry(Base):`
- [x] commit 181a65e exists (migration 016)
- [x] commit 419c06f exists (BpFundEntry model)
