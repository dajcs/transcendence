---
phase: "06.1"
plan: "03"
status: complete
completed_at: "2026-04-20"
---

# Plan 06.1-03 Summary — Service-Layer KP→LP Rename + Economy Formula Fixes

## What Was Built

Full service-layer rename from KpEvent/KP to LpEvent/LP across all 14 target files. Three economy formula fixes applied.

## Key Changes

### economy_service.py
- `compute_bet_cap()` function deleted entirely
- `get_balance()` returns `"lp"` key (was `"kp"`)
- `convert_kp_to_bp()` renamed to `convert_lp_to_bp()` with `min(math.log2(lp+1), 10.0)` cap
- `LpEvent` used throughout (was `KpEvent`)

### bet_service.py
- `KpEvent` import removed
- `compute_bet_cap` import removed
- `_check_bet_cap()` replaced with flat 10 BP cap (`amount > 10`)
- Proposer rebate (`own_bet_vote`) removed from `place_bet()`

### comment_service.py / market_service.py
- `KpEvent` → `LpEvent` in upvote functions

### auth_service.py / oauth_service.py
- `convert_kp_to_bp` → `convert_lp_to_bp`
- `notify_kp_converted` → `notify_lp_converted`

### notification_service.py
- `notify_kp_converted` → `notify_lp_converted`
- `"kp_converted"` → `"lp_converted"` notification type
- `"KP converted"` → `"LP converted"` message

### gdpr_service.py
- `KpEvent` → `LpEvent` in select/delete; `"kp_events"` → `"lp_events"` export key

### ledger_service.py
- `kp_q` → `lp_q` (LpEvent query); `"kp_conversion"` → `"lp_conversion"`; `"kp_allocation"` → `"lp_allocation"`

### models/__init__.py
- Re-exports `LpEvent` (not `KpEvent`)

### Tests + seed
- `test_compute_bet_cap_formula` replaced with `test_convert_lp_to_bp_cap`
- `test_get_balance_empty` asserts `"lp"` key
- `test_socket.py`, `test_tasks.py`, `seed_dev.py`: `KpEvent` → `LpEvent`

## Commits
- `19f7f7d` feat(06.1-03): economy_service — convert_lp_to_bp with 10BP cap; bet_service — flat 10BP cap, no rebate
- `f71309d` feat(06.1-03): rename KpEvent→LpEvent across all caller services; notify_lp_converted; lp_allocation reason strings
- `0e257cb` test(06.1-03): update tests and seed for LpEvent rename; test_convert_lp_to_bp_cap

## Self-Check: PASSED
- No remaining `KpEvent`, `convert_kp_to_bp`, `compute_bet_cap`, or `notify_kp_converted` references in app/tests/scripts
- `economy_service.py` contains `convert_lp_to_bp` with `min(math.log2(lp+1), 10.0)` cap
- `bet_service.py` `_check_bet_cap` uses flat `amount > 10` guard
- Proposer rebate removed from `place_bet()`
