---
phase: 06-polish-compliance
plan: 03
type: execute
wave: 3
depends_on:
  - 06-01
  - 06-02
files_modified:
  - frontend/src/app/(auth)/login/page.tsx
  - frontend/src/app/layout.tsx
  - frontend/src/components/auth/LoginForm.tsx
  - frontend/src/components/auth/RegisterForm.tsx
  - frontend/src/components/nav/TopNav.tsx
  - frontend/src/app/(protected)/settings/page.tsx
autonomous: false
requirements:
  - COMP-04
  - COMP-05
  - AUTH-05

user_setup:
  - service: google_oauth
    why: "OAuth smoke test — Google login requires configured credentials in .env"
    env_vars:
      - name: GOOGLE_CLIENT_ID
        source: "Google Cloud Console → APIs & Services → Credentials → Create OAuth 2.0 Client ID"
      - name: GOOGLE_CLIENT_SECRET
        source: "Google Cloud Console → same credential page → Client Secret"
    dashboard_config:
      - task: "Add authorized redirect URI"
        location: "Google Cloud Console → OAuth 2.0 Client → Authorized redirect URIs"
        value: "https://localhost:8443/api/auth/oauth/google/callback"
  - service: github_oauth
    why: "OAuth smoke test — GitHub login requires configured credentials in .env"
    env_vars:
      - name: GITHUB_CLIENT_ID
        source: "GitHub → Settings → Developer settings → OAuth Apps → New OAuth App"
      - name: GITHUB_CLIENT_SECRET
        source: "GitHub → OAuth App → Generate a new client secret"
    dashboard_config:
      - task: "Set callback URL"
        location: "GitHub OAuth App settings → Authorization callback URL"
        value: "https://localhost:8443/api/auth/oauth/github/callback"

must_haves:
  truths:
    - "Chrome DevTools console shows zero errors and zero warnings on all main pages"
    - "Dark mode has no flash of unstyled content (FOUC) on page load"
    - "OAuth provider buttons appear on the login page when at least one provider is configured"
    - "Switching locale EN→FR→DE updates all visible UI text on the current page without a full reload"
    - "OAuthButtons renders correctly in both light and dark mode"
  artifacts:
    - path: "frontend/src/app/(auth)/login/page.tsx"
      provides: "Login page with OAuthButtons + dark mode + i18n"
    - path: "frontend/src/app/layout.tsx"
      provides: "Root layout with suppressHydrationWarning on html and body"
      contains: "suppressHydrationWarning"
  key_links:
    - from: "frontend/src/app/layout.tsx"
      to: "frontend/src/components/ThemeProvider.tsx"
      via: "ThemeProvider wraps body"
      pattern: "ThemeProvider"
---

<objective>
Chrome audit pass and final OAuth verification. Fix any remaining console errors or hydration warnings found across all pages, verify OAuth buttons are wired correctly, and do a final pass for dark mode inconsistencies.

Purpose: COMP-04 requires zero Chrome console errors/warnings. AUTH-05 requires OAuth flows work end-to-end. This plan closes both with targeted fixes and a human verification checkpoint.

Output:
- Zero console errors on login, register, home, markets, dashboard, settings, privacy, terms
- OAuthButtons confirmed working: renders provider list, redirects correctly, error param shown on failure
- Any hydration warnings fixed (next-themes suppressHydrationWarning already in layout — verify still present)
- RegisterForm.tsx: ensure dark: classes on password input (Task 1 finding from phase context)
</objective>

<execution_context>
@/mnt/c/Users/dajcs/code/transcendence/.claude/get-shit-done/workflows/execute-plan.md
@/mnt/c/Users/dajcs/code/transcendence/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-polish-compliance/06-01-SUMMARY.md
@.planning/phases/06-polish-compliance/06-02-SUMMARY.md
@plan/AUTH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix dark mode and hydration issues found by audit</name>
  <files>
    frontend/src/app/(auth)/login/page.tsx
    frontend/src/components/auth/LoginForm.tsx
    frontend/src/components/auth/RegisterForm.tsx
    frontend/src/app/layout.tsx
    frontend/src/components/nav/TopNav.tsx
  </files>
  <read_first>
    - frontend/src/app/layout.tsx (verify suppressHydrationWarning present on html and body)
    - frontend/src/components/auth/RegisterForm.tsx (check for missing dark: on inputs)
    - frontend/src/components/auth/LoginForm.tsx (check password input — line 86 has no dark: classes)
    - frontend/src/app/(auth)/login/page.tsx (verify dark mode on page wrapper)
    - frontend/src/components/nav/TopNav.tsx (verify no stale hardcoded strings after i18n merge)
  </read_first>
  <action>
    Audit each file listed and apply the following targeted fixes:

    Fix 1 — layout.tsx: Confirm `suppressHydrationWarning` is on BOTH `<html>` and `<body>`.
    Current (already in place):
    ```tsx
    <html lang="en" suppressHydrationWarning>
      <body className="..." suppressHydrationWarning>
    ```
    If either is missing, add it. This is required for next-themes to work without hydration errors.

    Fix 2 — LoginForm.tsx: The password input (line ~86) is missing dark: classes.
    Find:
    ```tsx
    className="mt-1 block w-full border border-gray-300 rounded px-3 py-2 text-sm"
    ```
    Replace with:
    ```tsx
    className="mt-1 block w-full border border-gray-300 rounded px-3 py-2 text-sm dark:border-slate-600 dark:bg-slate-700 dark:text-gray-100"
    ```

    Fix 3 — RegisterForm.tsx: Check all input fields for missing dark: classes.
    Each `<input>` field should have `dark:border-slate-600 dark:bg-slate-700 dark:text-gray-100`.
    Each `<textarea>` (if any) should have the same dark: classes.
    Add missing dark: classes to any input without them.

    Fix 4 — TopNav.tsx: After imp/i18n merge, TopNav may have both the old hardcoded strings AND new useT() calls. Check for any remaining hardcoded nav strings ("Dashboard", "Markets", "Friends", "Chat", "Settings", "Login", "Sign Up", "Logout") that should be translated via useT(). If useT() is present from the merge, leave as-is. If hardcoded strings remain, replace with:
    ```tsx
    const t = useT();
    // ...
    {t("nav.dashboard")}  // instead of "Dashboard"
    {t("nav.markets")}    // instead of "Markets"
    {t("nav.friends")}    // instead of "Friends"
    {t("nav.chat")}       // instead of "Chat"
    {t("nav.settings")}   // instead of "Settings"
    {t("nav.login")}      // instead of "Login"
    {t("nav.signup")}     // instead of "Sign Up"
    {t("nav.logout")}     // instead of "Logout"
    ```
    Add `import { useT } from "@/i18n";` if not already present.

    Fix 5 — login/page.tsx: The page container has no dark: background. The page body handles this globally, so this is acceptable. However, verify the "No account?" / "Forgot password?" text uses dark: on the link:
    ```tsx
    // Already present from existing code:
    className="text-blue-600 hover:underline dark:text-blue-400"
    ```
    If the "No account?" paragraph text is `text-gray-600` without `dark:text-gray-400`, add it.
  </action>
  <verify>
    <automated>grep "suppressHydrationWarning" /mnt/c/Users/dajcs/code/transcendence/frontend/src/app/layout.tsx</automated>
    <automated>grep "dark:bg-slate-700" /mnt/c/Users/dajcs/code/transcendence/frontend/src/components/auth/LoginForm.tsx</automated>
    <automated>grep "dark:bg-slate-700" /mnt/c/Users/dajcs/code/transcendence/frontend/src/components/auth/RegisterForm.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep "suppressHydrationWarning" frontend/src/app/layout.tsx` returns 2 matches (html and body)
    - `grep "dark:bg-slate-700" frontend/src/components/auth/LoginForm.tsx` returns at least 1 match (password input now has dark: classes)
    - `grep "dark:bg-slate-700" frontend/src/components/auth/RegisterForm.tsx` returns at least 2 matches (multiple inputs)
    - `grep -E "useT|nav\." frontend/src/components/nav/TopNav.tsx` returns matches (nav strings translated)
    - No input element in LoginForm.tsx or RegisterForm.tsx has only `border-gray-300` without a corresponding `dark:border-slate-600`
  </acceptance_criteria>
  <done>Dark mode gaps in auth forms fixed. Hydration suppression confirmed. Nav strings translated via useT().</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Plans 01, 02, and 03/Task 1 delivered:
    - imp/i18n merged: useT() hook, locale store, EN/FR/DE dictionaries, 20+ pages wired
    - fix/polish merged: dark: variants on markets/settings pages
    - Privacy Policy at /privacy: renders in EN/FR/DE based on locale
    - Terms of Service at /terms: renders in EN/FR/DE based on locale
    - Language switcher (EN/FR/DE) in nav bar
    - OAuthButtons in login page with dark mode fix
    - Auth form inputs: dark mode on all fields
    - OAUTH_REDIRECT_BASE in .env.example
    - Dark mode toggle in nav
  </what-built>
  <how-to-verify>
    Prerequisites: Docker stack running (`docker compose up --build`).

    Step 1 — Dark mode:
    a. Open https://localhost (or https://localhost:3000) in Chrome
    b. Click the moon/sun icon in the top nav to toggle dark mode
    c. Verify: page background switches from white/gray to dark slate, all text remains readable
    d. Open DevTools → Console — verify zero errors, zero warnings

    Step 2 — Language switcher:
    a. With the app open, click EN / FR / DE buttons in the nav
    b. Navigate to the home page — verify the tagline and buttons change language
    c. Navigate to /privacy — verify the Privacy Policy heading and section titles change language
    d. Navigate to /terms — verify the Terms of Service heading changes language
    e. Refresh the page — verify the selected language persists (localStorage)

    Step 3 — OAuth buttons on login page:
    a. Navigate to /login
    b. If OAuth credentials are configured in .env: verify Google/GitHub/42 buttons appear below the login form, styled correctly in both light and dark mode
    c. If OAuth credentials are NOT configured: verify no OAuth buttons appear (providers array is empty), and no console error is thrown
    d. Click a configured OAuth provider button — verify redirect to the provider's authorization page

    Step 4 — Chrome console audit on all pages:
    a. Open each of these pages and check DevTools console for errors/warnings:
       - / (home)
       - /login
       - /register
       - /dashboard (authenticated)
       - /markets (authenticated)
       - /settings (authenticated)
       - /privacy
       - /terms
    b. Expected: zero red errors, zero yellow warnings

    Step 5 — GDPR on settings page:
    a. Go to /settings while logged in
    b. Verify "Export Data" button is present
    c. Verify "Delete Account" button is present
    d. Click "Export Data" — verify a JSON file downloads named `voxpopuli-data-export.json`

    Step 6 — Dark mode on legal pages:
    a. Toggle to dark mode
    b. Navigate to /privacy and /terms
    c. Verify: dark slate background, light text, table borders visible in dark mode
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe specific issues found (e.g., "console error: X on page Y", "French translation missing on /privacy", "OAuth button not visible")</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User browser → Next.js frontend | Hydration, client-side rendering |
| OAuth callback error param → login page display | error param from OAuth callback shown in UI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-09 | Injection | login/page.tsx: oauthError from searchParams | mitigate | oauthError is rendered as text content inside `<p>` tag (not dangerouslySetInnerHTML), so XSS is not possible — React escapes string content automatically |
| T-06-10 | Spoofing | OAuthButtons provider redirect | accept | href points to `${apiBase}/api/auth/oauth/${provider}` — apiBase is from env var set at build time, not user-supplied; provider key comes from backend /api/auth/oauth/providers response |
| T-06-11 | Information Disclosure | Chrome DevTools console | mitigate | Ensure no sensitive data (tokens, keys, user PII) is logged to console; fix any console.log statements found during audit |
</threat_model>

<verification>
Full Phase 6 verification after checkpoint approval:
- Chrome DevTools console: zero errors on /, /login, /register, /dashboard, /markets, /settings, /privacy, /terms
- Language switcher changes: home tagline, /privacy heading, /terms heading
- Locale persists on page refresh (localStorage key "locale")
- Dark mode toggle works on all pages; no FOUC
- OAuth provider buttons appear on /login when credentials configured
- GDPR export downloads valid JSON from /settings
- account deletion works via DELETE /api/users/account
</verification>

<success_criteria>
COMP-04: Zero Chrome console errors or warnings on all main pages in both light and dark mode.
AUTH-05: OAuth buttons appear for configured providers; redirect to provider auth page works; callback sets auth cookies and redirects to /dashboard.
All Phase 6 requirements verified: AUTH-05, COMP-01, COMP-02, COMP-03, COMP-04, COMP-05, COMP-06.
</success_criteria>

<output>
After completion, create `.planning/phases/06-polish-compliance/06-03-SUMMARY.md`
</output>
