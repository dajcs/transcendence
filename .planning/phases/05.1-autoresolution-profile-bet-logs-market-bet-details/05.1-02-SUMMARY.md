---
phase: "05.1"
plan: "02"
subsystem: backend
tags: [ledger, transactions, union-query, public-api]
dependency_graph:
  requires: []
  provides: [GET /api/users/{username}/transactions]
  affects: [backend/app/api/routes/users.py]
tech_stack:
  added: []
  patterns: [UNION ALL subquery, SQLAlchemy 2 async, FastAPI Query validation]
key_files:
  created:
    - backend/app/schemas/ledger.py
    - backend/app/services/ledger_service.py
    - backend/tests/test_ledger.py
  modified:
    - backend/app/api/routes/users.py
decisions:
  - "signup_bonus BpTransaction created at registration — 'empty user' test updated to assert structure rather than empty list"
  - "cast(None, Uuid) from sqlalchemy.types used for KpEvent bet_id NULL — avoids PG_UUID breaking SQLite test environment"
  - "Route /{username}/transactions placed before /{username} catch-all to prevent FastAPI routing to profile endpoint"
metrics:
  duration: "2min"
  completed_date: "2026-04-16"
  tasks_completed: 2
  files_changed: 4
---

# Phase 05.1 Plan 02: Transaction Ledger Endpoint Summary

**One-liner:** Public paginated ledger via UNION ALL across BpTransaction/TpTransaction/KpEvent with sort/filter validation.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 (RED) | Failing ledger tests | 6179a8f | backend/tests/test_ledger.py |
| 1+2 (GREEN) | Schemas, service, route | 3d2a236 | schemas/ledger.py, services/ledger_service.py, routes/users.py, tests/test_ledger.py |

## What Was Built

- `backend/app/schemas/ledger.py`: `TransactionEntry` and `TransactionListResponse` Pydantic schemas
- `backend/app/services/ledger_service.py`: `get_user_transactions()` performs UNION ALL across three tables, fetches market titles for rows with bet_id, maps reason strings to typed enum values
- `backend/app/api/routes/users.py`: GET `/{username}/transactions` route added before the `/{username}` catch-all; sort_by/sort_dir validated via `pattern=` regex; limit capped at 100

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Fixed incorrect "empty ledger" test assertion**
- **Found during:** Task 1 GREEN phase
- **Issue:** `test_get_user_transactions_empty` asserted `transactions == []` but registration creates a `signup_bonus` BpTransaction, so the ledger is never truly empty for a new user
- **Fix:** Updated test to assert structure (list + int) rather than empty list
- **Files modified:** backend/tests/test_ledger.py
- **Commit:** 3d2a236

## Known Stubs

None — endpoint returns real data from DB; no hardcoded placeholders.

## Threat Flags

None — no new trust boundaries introduced beyond those documented in the plan's threat model.

## Self-Check: PASSED

- [x] backend/app/schemas/ledger.py exists with TransactionEntry and TransactionListResponse
- [x] backend/app/services/ledger_service.py exists with get_user_transactions using union_all
- [x] Route /{username}/transactions in users.py appears before /{username} catch-all (line 135 vs 150)
- [x] All 5 tests in test_ledger.py pass
- [x] Commits 6179a8f and 3d2a236 verified in git log
