---
phase: 05-intelligence-resolution
plan: 09
type: execute
wave: 6
depends_on: [05-06]
files_modified:
  - frontend/src/app/(protected)/markets/new/page.tsx
autonomous: true
gap_closure: true
requirements: []

must_haves:
  truths:
    - "Market creation deadline field allows setting both date AND time without manual text entry"
    - "Date and time inputs are visually distinct and labeled clearly"
    - "Combined value is submitted as ISO datetime string to the backend (same format as before)"
  artifacts:
    - path: "frontend/src/app/(protected)/markets/new/page.tsx"
      provides: "Separate date + time inputs for deadline"
      contains: "type=\"date\""
  key_links:
    - from: "frontend/src/app/(protected)/markets/new/page.tsx"
      to: "deadline state"
      via: "combined date+time onChange handlers"
---

<objective>
Fix the market creation deadline UX: the current `datetime-local` input requires the user to type the time manually. Replace it with two separate `<input type="date">` and `<input type="time">` fields that combine into a single ISO datetime string for state/submission.

Purpose: Addresses UAT gap from Phase 5 human verification — users couldn't easily set the deadline time.
Output: Improved deadline UX on the new market form.
</objective>

<execution_context>
@/mnt/c/Users/dajcs/code/transcendence/.claude/get-shit-done/workflows/execute-plan.md
@/mnt/c/Users/dajcs/code/transcendence/.claude/get-shit-done/templates/summary.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Split datetime-local into separate date + time inputs</name>
  <files>frontend/src/app/(protected)/markets/new/page.tsx</files>
  <read_first>
    - frontend/src/app/(protected)/markets/new/page.tsx (FULL FILE)
  </read_first>
  <action>
Read the full file. The current deadline field is around line 112-121:
```tsx
<div className="space-y-1">
  <label className="text-sm font-medium">Deadline</label>
  <input
    required
    type="datetime-local"
    value={deadline}
    onChange={(e) => setDeadline(e.target.value)}
    className="w-full rounded border border-gray-300 px-3 py-2 text-sm"
  />
</div>
```

The `deadline` state currently holds a datetime-local string (e.g. "2026-04-15T14:30").

**Change:**

1. Add two derived state pieces from the existing `deadline` state. Do NOT add new state variables — derive date and time parts from the existing `deadline` string:

```tsx
const deadlineDate = deadline.split("T")[0] ?? "";
const deadlineTime = deadline.split("T")[1] ?? "12:00";
```

2. Replace the single `datetime-local` input with two side-by-side inputs:

```tsx
<div className="space-y-1">
  <label className="text-sm font-medium">Deadline</label>
  <div className="flex gap-2">
    <div className="flex-1 space-y-0.5">
      <label className="text-xs text-gray-500">Date</label>
      <input
        required
        type="date"
        value={deadlineDate}
        onChange={(e) => setDeadline(`${e.target.value}T${deadlineTime}`)}
        className="w-full rounded border border-gray-300 px-3 py-2 text-sm"
      />
    </div>
    <div className="flex-1 space-y-0.5">
      <label className="text-xs text-gray-500">Time</label>
      <input
        required
        type="time"
        value={deadlineTime}
        onChange={(e) => setDeadline(`${deadlineDate}T${e.target.value}`)}
        className="w-full rounded border border-gray-300 px-3 py-2 text-sm"
      />
    </div>
  </div>
</div>
```

The `deadline` state still holds the combined "YYYY-MM-DDTHH:mm" string that gets submitted to the backend — no backend change needed.

Make only this change. Do not touch anything else in the file.
  </action>
  <verify>
    <automated>grep -n "type=\"date\"\|type=\"time\"" /mnt/c/Users/dajcs/code/transcendence/frontend/src/app/\(protected\)/markets/new/page.tsx</automated>
  </verify>
  <done>Two separate date and time inputs present, deadline state still combines them into a single ISO string.</done>
  <acceptance_criteria>
    - `grep 'type="date"' frontend/src/app/(protected)/markets/new/page.tsx` returns match
    - `grep 'type="time"' frontend/src/app/(protected)/markets/new/page.tsx` returns match
    - `grep 'datetime-local' frontend/src/app/(protected)/markets/new/page.tsx` returns no match
  </acceptance_criteria>
</task>

</tasks>

<verification>
grep -n "type=\"date\"\|type=\"time\"\|datetime-local" /mnt/c/Users/dajcs/code/transcendence/frontend/src/app/\(protected\)/markets/new/page.tsx
</verification>

<success_criteria>
- Separate date and time inputs replace datetime-local
- Combined deadline value still submitted as ISO string
- No other changes to the form
</success_criteria>

<output>
After completion, create `.planning/phases/05-intelligence-resolution/05-09-SUMMARY.md`
</output>
