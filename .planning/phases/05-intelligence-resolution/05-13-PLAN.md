---
phase: 05-intelligence-resolution
plan: 13
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/app/(protected)/markets/[id]/page.tsx
  - frontend/package.json
autonomous: true
gap_closure: true
requirements: [LLM-01, LLM-02]

must_haves:
  truths:
    - "AI summary text renders with markdown formatting (bold, italic, lists) not as a raw string"
    - "AI hint text renders with markdown formatting not as a raw string"
  artifacts:
    - path: "frontend/src/app/(protected)/markets/[id]/page.tsx"
      provides: "ReactMarkdown wrapping both summary and hint display points"
      contains: "ReactMarkdown"
    - path: "frontend/package.json"
      provides: "react-markdown dependency"
      contains: "react-markdown"
  key_links:
    - from: "frontend/src/app/(protected)/markets/[id]/page.tsx"
      to: "react-markdown"
      via: "import ReactMarkdown from 'react-markdown'"
      pattern: "ReactMarkdown"
---

<objective>
Render AI-generated summary and hint responses as markdown in the market detail page.

Purpose: LLM responses use markdown formatting (bold phrases, lists, line breaks). Displaying them as plain text strings makes the output look cluttered with raw asterisks and symbols. Installing react-markdown and wrapping the two display points makes the output readable.
Output: react-markdown installed; summary and hint wrapped in ReactMarkdown with Tailwind prose classes.
</objective>

<execution_context>
@/mnt/c/Users/dajcs/code/transcendence/.claude/get-shit-done/workflows/execute-plan.md
@/mnt/c/Users/dajcs/code/transcendence/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Exact current JSX at the two render points. Use directly. -->

Summary display (around page.tsx line 862-864):
```tsx
<div className="rounded bg-gray-50 border border-gray-200 p-3 text-sm text-gray-800 space-y-1">
  <p className="text-xs font-medium text-gray-500">AI Summary</p>
  <p>{summary}</p>          {/* <-- replace this <p> */}
  <button onClick={() => setSummary(null)} ...>Refresh</button>
</div>
```

Hint display (around page.tsx line 570-574):
```tsx
{hint && (
  <div className="rounded bg-blue-50 border border-blue-200 p-2 text-sm text-blue-900">
    {hint}                  {/* <-- replace this bare text node */}
  </div>
)}
```

Target replacements:
```tsx
// Summary — replace <p>{summary}</p> with:
<ReactMarkdown className="prose prose-sm max-w-none">{summary}</ReactMarkdown>

// Hint — replace {hint} with:
<ReactMarkdown className="prose prose-sm max-w-none">{hint}</ReactMarkdown>
```

Import to add at top of page.tsx:
```tsx
import ReactMarkdown from 'react-markdown';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install react-markdown</name>
  <files>frontend/package.json</files>
  <action>
From the `/mnt/c/Users/dajcs/code/transcendence/frontend` directory, run:

```bash
npm install react-markdown
```

This adds react-markdown to package.json dependencies and updates package-lock.json. react-markdown is an ESM-only package compatible with Next.js App Router (no special config needed).

Do NOT install @tailwindcss/typography separately — the `prose` classes already work if the plugin is configured, but if it is not configured, the className is simply ignored without breaking anything. Do not modify tailwind config.
  </action>
  <verify>
    <automated>cd /mnt/c/Users/dajcs/code/transcendence/frontend && grep '"react-markdown"' package.json</automated>
  </verify>
  <done>package.json dependencies contains a react-markdown entry.</done>
</task>

<task type="auto">
  <name>Task 2: Wrap summary and hint with ReactMarkdown</name>
  <files>frontend/src/app/(protected)/markets/[id]/page.tsx</files>
  <action>
Three targeted edits to page.tsx:

**Edit 1 — Add import** at the top of the file, alongside the other named imports (after the existing import block, before the component):
```tsx
import ReactMarkdown from 'react-markdown';
```

**Edit 2 — Summary display** (search for `<p>{summary}</p>` inside the AI Summary div):
Replace:
```tsx
<p>{summary}</p>
```
With:
```tsx
<ReactMarkdown className="prose prose-sm max-w-none">{summary}</ReactMarkdown>
```

**Edit 3 — Hint display** (search for the hint div: `rounded bg-blue-50 border border-blue-200 p-2 text-sm text-blue-900`):
Replace the content of that div from the bare `{hint}` text node to:
```tsx
<ReactMarkdown className="prose prose-sm max-w-none">{hint}</ReactMarkdown>
```

The surrounding `<div>` tags, classNames, and conditional `{hint && ...}` wrapper must remain unchanged. Only replace the inner content at each of the two display points.
  </action>
  <verify>
    <automated>cd /mnt/c/Users/dajcs/code/transcendence/frontend && grep -n "ReactMarkdown" src/app/\(protected\)/markets/\[id\]/page.tsx</automated>
  </verify>
  <done>grep returns at least 3 lines: the import statement plus two JSX usages wrapping summary and hint respectively.</done>
</task>

</tasks>

<verification>
After both tasks complete:

```bash
# Confirm import and both usages present
cd /mnt/c/Users/dajcs/code/transcendence/frontend
grep -n "ReactMarkdown" src/app/\(protected\)/markets/\[id\]/page.tsx
# Expected: 3+ lines (import + 2 usages)

# Confirm package installed
grep '"react-markdown"' package.json

# Type-check passes
npm run type-check
```
</verification>

<success_criteria>
- react-markdown present in frontend/package.json dependencies
- ReactMarkdown imported in page.tsx
- Summary display point uses ReactMarkdown instead of bare `<p>{summary}</p>`
- Hint display point uses ReactMarkdown instead of bare `{hint}` text node
- `npm run type-check` exits 0 (no TypeScript errors)
</success_criteria>

<output>
After completion, create `.planning/phases/05-intelligence-resolution/05-13-SUMMARY.md`
</output>
