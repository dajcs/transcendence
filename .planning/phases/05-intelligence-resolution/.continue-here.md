---
phase: fix/logic (post-milestone UAT fixes)
task: pending human UAT
total_tasks: 3 fixes done, 1 human test remaining
status: paused — awaiting UAT
last_updated: 2026-04-05T23:34:35.135Z
---

<current_state>
All 5 GSD phases complete. On branch fix/logic, performing post-milestone UAT.
Phase 05 UAT test 4 (Socket events reach browser from Celery) was run manually.
User found 3 sub-issues. All 3 have been fixed in code. Changes NOT YET COMMITTED.
Waiting for user to run docker compose up --build and verify fixes.
</current_state>

<completed_work>

- Fix 1: Market listing real-time updates — DONE
  `frontend/src/app/(protected)/markets/page.tsx`
  Added `bet:status_changed` + `bet:resolved` socket listeners to `MarketsPage`.
  Calls `queryClient.invalidateQueries({ queryKey: ["markets"] })` on event.
  Markets listing now refreshes in-place without needing to click into a detail page.

- Fix 2: Deadline resolution delay (~5 min) — DONE
  `backend/app/workers/celery_app.py` — changed beat from `crontab(minute="*/5")` to `schedule=60.0`
  `backend/app/workers/tasks/resolution.py` — `check_auto_resolution` now calls `_process_auto_resolution` inline; removed `_scan_and_enqueue_expired_bets` double-hop
  Max deadline-to-resolution delay reduced from ~5 min to ~1 min.

- Fix 3: Browser notification click navigates to market — DONE
  `frontend/src/components/NotificationBell.tsx`
  Added `showBrowserNotification(body, url)` helper with `n.onclick` handler.
  `resolution_due` navigates to `/markets/{market_id}`.
  `bet_resolved` and `bet_disputed` navigate to `/markets/{bet_id}`.
  Handler definitions reordered so `showBrowserNotification` is declared before use.
</completed_work>

<remaining_work>

- Human UAT: run `docker compose up --build` and test:
  1. Open /markets in two browser tabs. Wait for a deadline to pass. Listing in second tab should update without clicking through to detail.
  2. Create market with deadline ~2 min from now. Wait. Resolution should trigger within ~1 min of deadline (not 5 min).
  3. Allow browser notifications. Trigger a resolution_due or bet_resolved event. Click the browser notification popup -> should open /markets/{id} directly.

- After UAT passes: commit all 5 modified files and update `05-HUMAN-UAT.md` test 4 result to `pass`, status to `resolved`.
</remaining_work>

<decisions_made>

- `schedule=60.0` over `crontab(minute="*/5")`: crontab fires at fixed clock ticks (:00, :05, :10...) so worst case is ~5 min. Celery's float schedule fires every N seconds from last completion — max ~1 min.
- Inline resolution in sweep instead of re-enqueueing: removes extra queue latency from double-hop; `_process_auto_resolution` has idempotency guard (checks `status == "open"` before acting).
- `window.focus()` before `router.push` in notification onclick: brings the browser tab to front when user clicks a system notification.
</decisions_made>

<blockers>
None — all code changes complete, pending human verification only.
</blockers>

<context>
Branch fix/logic. All phases done. The ETA task (per-market Celery task at deadline) was already in place but the 5-min sweep was the fallback catching most cases. The sweep interval reduction + inline execution is the main timing fix. The listing page socket fix is the most impactful UX change. Notification onclick was a simple missing handler.

Note: `frontend/src/app/page.tsx` (homepage tagline) was already modified before this session — not related to these fixes.

Previous .continue-here.md context (from last GSD session):
05-06 (socket wiring + navbar /settings link) was the last remaining GSD plan. That work appears to have been done in the "login" commit (b3fa641) and the "event-driven-celery" commit (fa73644) based on commit history. The prior UAT handoff notes are superseded by the current fix/logic work.
</context>

<next_action>
Run `docker compose up --build` -> test 3 issues -> if pass, commit with message "fix: real-time listing updates, 60s resolution sweep, notification click navigation" -> update 05-HUMAN-UAT.md test 4 to pass.
</next_action>
