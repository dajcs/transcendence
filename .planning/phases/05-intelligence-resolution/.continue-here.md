---
phase: 05-intelligence-resolution
task: 1
total_tasks: 1
status: paused
last_updated: 2026-04-01T15:11:53.173Z
---

<current_state>
Phase 05 plan 05-06 not yet started. Plans 01-05 + 15 hotfixes committed. Tree is clean.
Community vote UX is complete. Scheduling replaced with ETA callbacks.
</current_state>

<completed_work>

- 05-01 through 05-05: test scaffolds, resolution service, LLM service, Celery tasks, resolution UI
- hotfix-6 through hotfix-10: SQLAlchemy fix, status colors, active positions, Place Bet guard, Tier 2.5 voting
- hotfix-11: Your Position hides withdraw/refund when market not open; Community Vote opinion-based buttons per market type; backend: removes yes/no constraint, vote_weights dict, upsert cast_vote, user_vote in DisputeRecord
- hotfix-12: Replace 5-min Celery poll with per-market ETA task (resolve_market_at_deadline); per-dispute close_dispute_at_deadline ETA callback; notify_resolution_due bell notification; dashboard URL tab param; NotificationBell resolution_due type
- hotfix-13: Active button green+bold+border-2; tally ordered with 0-weight entries; weight format (x.x) not "xw"; close_dispute_at_deadline also from _escalate_overdue_proposer
- hotfix-14: Vote weight bug fix — compare body.vote vs user position (was: position vs resolution.outcome); all active buttons green
- hotfix-15: Numeric community vote: own vote+weight + sorted bar chart; YES button red when voted NO; user_weight in DisputeRecord API + TypeScript type
</completed_work>

<remaining_work>

- Plan 05-06 (1 task): Wire socket events on market detail page:
  - bet:resolved → show payoutBanner
  - resolution:proposed → invalidate market + resolution queries
  - resolution:review_updated → invalidate resolution query (live vote counts)
  - dispute:opened → invalidate market + resolution queries
  - dispute:voted → patch vote_weights in resolution query cache (was: yes/no weight, now: vote_weights dict)
  - dispute:closed → invalidate market query
- Add /settings link to Navbar
</remaining_work>

<decisions_made>

- Community vote is opinion-based (what YOU think is correct), not agree/disagree with proposer
- Vote weight: compare user VOTE vs user BET POSITION (match→0.5, differ→2.0, no position→1.0)
- vote_weights: Record<string, float> replaces yes_weight/no_weight throughout API
- Dispute votes are upsert — user can change vote by clicking another button
- 5-min Celery beat poll → per-market ETA task at deadline+5min
- Per-dispute close_dispute_at_deadline ETA task at closes_at (48h)
- 15-min dispute deadline poll retained as safety net
- Numeric community vote: own vote+weight + bar chart sorted by value
- Binary: YES button turns red when user voted NO
- Dashboard reads ?tab=my_markets from URL on mount (window.location.search pattern)
- NotificationBell: resolution_due type → navigates to /dashboard?tab=my_markets
</decisions_made>

<blockers>
None blocking.
</blockers>

<context>
Two sessions of community vote UX iteration + scheduling architecture changes.
Tier 3 dispute is now a proper opinion poll with market-type-aware inputs and correct weight logic.
Scheduling uses exact-time ETA Celery callbacks instead of polling.
05-06 is the last plan — small (6 socket listeners + navbar link).
NOTE: dispute:voted socket payload now emits vote_weights dict (not yes_weight/no_weight) —
the 05-06 socket handler must patch resolutionQuery cache with the new vote_weights shape.
</context>

<next_action>
/clear then /gsd:execute-phase 05 — executes plan 05-06 (socket wiring + navbar /settings link).
After 05-06, run /gsd:verify-work for Phase 5.
</next_action>
